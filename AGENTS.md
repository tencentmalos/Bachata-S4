## 当前仓库主分支（用户指定，2026-09-20）

- `malos/main` 是本仓库当前主分支，用于集成各 feature 的改动。
- 后续创建新的 feature / 开发分支时，默认以最新的 `malos/main` 为基线；用户明确指定其他基线时遵循用户要求。
- `malos/main` 从 `feature/malos/stream_texture_support` 的当前 HEAD 创建。下方历史记录中的分支名仅描述当时状态，当前主分支约定以本节为准。
- 本轮资源策略、VMA 统计和血源 TextureCache 死锁修复随 `malos/main` 本次提交归档；下方记录及 evidence manifest 中的“本地未提交”是验证时点状态，未完成的 spec 验收项仍然有效。

- **血源 TextureCache 死锁修复／资源 spec 续项（2026-09-20，本地未提交）：** [根因、修复和验收](docs/validation/android-native-host/resource-policy-remaining-20260920.md)。Swan旧PID14725/gen1真实栈确认 FindImage→ResolveOverlap→UpdateImage 重入同一非递归mutex，非Turnip/KGSL结论。extent分支改锁内helper并核对格式/layout兼容；退休内部回读隔离plan，尺寸往返不继承临时native；补>32MiB图像回读buffer所有权+invalidate。真实TextureCache两驱动各20/0，原GPU各13,327,401/0、CPU34/0；保留空Rasterizer夹具崩溃与往返1fail反例。用户批准结束旧死锁后安装APK6d89758b/hostc07bdea3/JNI105c3d53，Swan PID16849/gen1诊所人物/HUD可见、MOVE/相机响应，4286presents后正常UIStop1.771s/user_stop/return0/TracerPid0。旧attachment_passes实为draw；新版v2分draw/实际rendering scope，128组显式overflow。血源累计draw5.2429%/scope9.8766%含加载动画，native readback/mixed-pass仍需追踪，不是固定场景A/B。保留用户Render0.5/TextureMedium与Turnip；最终无活动游戏/输入/采集/调试器。有效mip、部分dirty、small-RT准入、完整回读与LOD/Grad及覆盖率门仍未完成，无整spec/全游戏/提速/总RAM改善声明。

- **资源策略 / VMA内存档位（2026-09-20，本地未提交）：** [实现、测量与边界](docs/validation/android-native-host/resource-policy-vma-20260920.md)。Render/Texture独立，默认Render0.5/TextureHigh；Low对准入资产真正省略host mip0，并让staging/detile仅处理保留尾链，小图保护、首次附件重规划、复制继承与保守native回退。VMA账本覆盖全部本仓VMA分配，区分live/reserved/free/retiring、峰值/分类/实际块释放、RSS/PSS/SwapPss；不是整个Host CPU物理RAM。材质高/中/低首选块256/128/64MiB、staging512/256/128MiB，中低紧凑分配+闲置上传资产回收，重启生效；不提前退休或强制GPU idle。Qualcomm/Turnip各13,327,401/0，CPU34/0、Kotlin125/0、SPIR-V3/3；80MiB burst高/低峰值96/120MiB而idle64/8MiB，不能保证所有峰值更低。新APK9d6504bf/hoste4414aca/JNI5c82b208已安装Swan，PID10156/gen1屋顶移动→ATTACK提示，正常UIStop/user_stop/return0/TracerPid0。新Low屋顶reserved2043.84–2171.84MiB，非退役live1319.66MiB，utility1248.06→864.06MiB明确省384MiB staging；PSS/SwapPss独立，不作总RAM/FPS收益声明。原spec未完成：小RT保护使同版legacy99.7623%→High98.5735%缩放draw（旧计数名误写pass，后续已纠正），覆盖率门未过；Bloodborne/真实streaming/DRS和完整性能画质对照待验。临时override已删，全局原样Render0.5/High、legacy空、Turnip保留，无活动游戏；不把定向通过等同整份spec完成。

- **Orbis 同步量化 / 交接（2026-09-20）：** [交接入口](docs/handoffs/android-orbis-sync-20260920.md)、[实现和证据](docs/validation/android-native-host/orbis-sync-metrics-20260920.md)。新增默认OFF的`hle_sync status|dump|start/detail/stop <context>`：分线程累计次数/elapsed/直方图/结果，500ms静态名Litep counters，detail Lookup/Guard/Park/Publish/Reacquire及等待入口定位。elapsed含等待非CPU，rolling snapshot，PhaseMask0表示未覆盖。POSIX sem/rw每对象CV+取消通知转交；32无关操作Android33/34→1/1、Linux34/33→1/1，保留domain metadata锁。metrics Android/TSan45/0、rw60/0、mutex65499/0、Android/Linux/TSan各64轮取消竞态通过。最终APK3ba6e584/host914dd36f已安装；用户要求优先提交，新增counter真实游戏PROF/MCP消费和最终APK实景验收尚未完成，无FPS结论。设备Settings选0.5（默认仍0.5），无活动游戏；HLE FEX app-shipped快路径仅评估计划，不能放游戏patch。 离线评估另附三组历史实景PROF+匹配KGSL/sched ZIP（约304MiB），[交付清单](docs/handoffs/android-orbis-sync-traces-20260920.json)及跨机器加载方法见交接文档；不作为新增HLE统计验收。

- **Internal Scale 低倍率（2026-09-20）：** [实现与验证](docs/validation/android-native-host/internal-scale-low-20260920.md)。Android 五档 0.25/0.375/0.5/0.75/1.0，默认仍 0.5；Settings → Graphics，重启生效。完整小数百分比/JNI、八分之一尺寸、状态栏、0.25 两级 mip-drop、ASTC 路径已接通；shader binary9。Runtime109+Settings13通过，Qualcomm/Turnip各3,134,415checks/0failures，SPIR-V3/3。新APK已按用户确认安装，当前0.5；未做新倍率全游戏性能/画质验收。

- **血源 TSC / Orbis semaphore 优化（2026-09-20）：** [实现、Citron对照与证据](docs/validation/android-native-host/bloodborne-tsc-20260920.md)。FEX SMALLTSCSCALE与全部Orbis TSC/ProcessCounter/VideoOut接口统一缩放，ARM原始频率直接读CNTFRQ；AYN19.2MHz→guest1.2288GHz，host微秒/调度/profiler不缩放。有效6.270s诊所PROF2.504M/246chunks/0skipped、49帧，KGSL512MiB/14s无overrun，6828次acquire→WaitSema中位10.428→0.1665ms，每次acquire CPU约0.608→0.124ms；原24s trace覆盖保留不用作完整CPU结论，context不匹配2646条排除。仅TSC普通同视角B/A复测8.306/7.260FPS约+14.42%，短时样本。随后Orbis semaphore改每waiter定向通知，保留选择/计数/取消/销毁协议及短domain mutex；旧反例588项2预期失败，无关wake76/78→2/3，Android588/0、LLVM TSan511/0、FEXclock5/0、services78/0、生产clock3轮通过。最终APK432129ae/hostf299a128/JNIbc53f543，普通PID5695/gen1诊所9.74–9.82FPS，镜头略变不归因全部增量；MOVE/相机可操作后正常UIStop，输入/探针/sync/GPUtiming/capture OFF、ringON、TracerPid0、tracefs清理；随后设备外部重启为PID8942/gen1 Running，未干预该新会话。GPU77.786ms独立wait及CPU生产者耗时仍在；POSIX sem_t未改；无完整游戏回归、全帧C++或完全去锁声明。

- **血源任务队列自旋定位（2026-09-20）：** [同窗证据与推进顺序](docs/validation/android-native-host/bloodborne-job-spin-20260920.md)。PID18248/gen2诊所可见后采12.222s、3.659M事件/360chunks/0skipped、60完整帧、242 EH探针，KGSL/sched完整覆盖/overrun0。队列DLAdaptiveMutex +0x2037cb0：固定200000ticks遇当前19.2MHz guest TSC，551次进入WaitSema前最短10.421/中位10.428ms，吻合10.417ms预算；acquire跨worker累计11.540s/CPU9.707s。105.365ms任务组等待的最后notifier中，取锁93.760ms、payload0.184ms。持锁释放节点+0x2033732累计1.763s，其中runnable1.517s；对象级同锁owner尚未关联。禁止单独恢复FEX TSC缩放，须与Orbis时钟同域；尚未部署预算/队列修复或声称提速。Guest batch补新SDK async wire、连续帧边界及worker根，35测试通过；工具7f9da023/local asyncjobs1已安装，真实MCP60帧/3根；11wrapper核验10通过，独立webaccess fetch失败；符号rev2共16函数/2结构体，recipe重现相同SHA。终态正常UIStop/user_stop、探针next-session清空/sync0/GPUoff/captureoff/ringon/TracerPid0，独立tracefs与本轮study workspace已清理。场景移动未严格验收，无全游戏回归。

- **锁拆分后场景 Litep/KGSL（2026-09-20）：** [同窗卡点分析](docs/validation/android-native-host/buffer-upload-litep-20260920.md)。已补齐下条尚未完成的 Litep 分析：实际诊所人物可见后，PID6163/gen2、APKc14cf608/host0d272251；有效capture2 PROF b575d658、KGSL da9be3ae，9.976s全覆盖，2.239M事件/222chunks/0skipped、42完整source帧、421GPU对/0orphan，sched overrun0。4.272FPS/234.061ms，GPU均值87.084ms，诊断扰动下非A/B。source4417长287.634ms，Guest19等待Guest1通知231.265ms，恢复+重获锁0.121ms；Guest1再等CSEzWork/Guest55 65.805ms，worker onCPU52.170/runnable8.223/sleep5.253ms。重现角色阶段+0x19186c5→task完成+0x203377c，任务虚表主体尚未分解。另647匹配flow，最大103.380ms排队对应submit worker先前KGSL wait105.521ms/onCPU0.049ms，不与CPU帧相加。loading早采与时间错位样本拒用于结论；最终自动armed→PROF保证同窗。优先标定实际task callback和调度竞争，不再盲改epoll/condition。终态Stopped/user_stop、sync0、GPUoff、captureoff、ringon、TracerPid0，独立tracefs已清理。无新生产优化/全回归/提速声明。

- **Buffer 上传锁拆分（2026-09-20）：** [实现/验证](docs/validation/android-native-host/buffer-upload-unlock-20260920.md)、[跨仓交付](docs/validation/android-native-host/git-delivery-20260920.md)。旧修改 f8d264e5 已推送，现分支 feature/malos/hle_vr。SnapshotForUpload 将 staging Map/分配/GPU wait 移出全部区域锁，锁内仅保护/复制/交接 dirty，容量重查最多两次，失败回滚；fault 侧不再遍历 renderer buffer_ranges。仍保留相关区域 memcpy 临界区和 GPU+host-submit 双完成退役，没有备用无限 staging 池。Android 14+44=58/0，host/APK 构建通过；APK c14cf608/host 0d272251/JNI 20ccbae6，PID6163/gen1 诊所可见移动，最终 Stopped/user_stop、TracerPid0。native10s8115样本/0lost，上屏5.695FPS，无提速或完整回归声明。Litep 同版本长帧因果分析仍待补齐，不能把 native 采样当完成。此条覆盖下文“上传仍跨等待持锁/尚未修复容器边界”的当前状态，保留历史证据。

- **血源 Buffer Cache 区域锁（2026-09-20，本地未提交）：** [实测/修复](docs/validation/android-native-host/bloodborne-buffer-tracking-locks-20260919.md)、[Azahar/Citron 对照与拆分方案](docs/validation/android-native-host/buffer-cache-concurrency-comparison-20260920.md)。Native 样本证明 guest 写保护 fault→BufferCache→RegionManager 的 Android 自旋热点；host 字节交换 self37.60%→0.03%，移动后慢窗口0.02%，不能等同整游戏提速。Android 改256KiB分区+原生futex、两级原子目录；32项检查通过，旧实现18项中2个预期失败。上传仍跨 on_upload 持多个区域锁，可能进入 staging/GPU wait；尚未拆除此耦合。CPU/GPU脏标记是数据一致性，staging退役是资源生命周期，后续应分离。Citron仍有cache粗锁，但上传区忙时使用备用pool；Azahar仍有串行边界，但按读写范围求依赖fence并合并失效。审计还发现fault侧读取renderer修改的buffer_ranges缺少可见容器同步，未归因崩溃。最终APK12990b6f/host9a39859b/JNI20ccbae6，PID13889/gen1正常UIStop/user_stop；静止5.525FPS、移动后1.887FPS，相机未严格匹配，无FPS修复声明。新增CUSA03023/01.00七个exact-SHA重建符号/dry-run通过，未部署探针或guest替换。无完整回归/commit/push；保留旧修改。

- **Swan ZAR / 最新 Turnip（2026-09-20，本地）：** [ZAR 验证](docs/validation/android-native-host/swan-zar-20260920.md)、[驱动与实景](docs/validation/android-native-host/swan-turnip-20260920.md)。本体/更新/DLC 分拆6个 ZAR 已传 Swan 并核 SHA，独立 guest-patch `.zar.db` 留待后续，未实现 SQLite patch reader。ART 低地址保留冲突按实际 host maps 切分并精确 advisory mmap，禁止覆盖 host。Mesa upstream e1f3f372 更新并 fork 到 tencentmalos/mesa-mirror；独立提交 be8ebc54（KGSL zero-timeout poll）与86ca472f（公开 Mapper5 RGB 元数据）已推送、发布。37 wait +328 metadata、真实 AHB/timeline、GPU3,134,415/0通过。Swan PID16866/gen1，APK295c2f87/hostebf76ed9/driverEA4853BF，TMNT 双 ZAR 屋顶移动→对话→攻击教学，22,210 presents 后正常 UIStop/user_stop、TracerPid0；该 PID 未见新 KGSL fault。首版 gralloc CCU 故障保留，输入诊断证明 guest 收到按键，未改输入语义。当前显式 mainline Turnip、Scale0.5；资源分流 spec 尚未生产实现，无全游戏/血源/Beat Saber验收、FPS或总内存改善结论。主仓交付分支 `feature/malos/stream_texture_support`，保留无关 `externals/dear_imgui`。

## KGSL / CPU 串行等待诊断入口（2026-09-19）

- **Swan ROM / KGSL 本机源码（用户要求，2026-09-20）：** Turnip 与 Swan 的 KGSL ioctl/property、context/fence/timestamp、GMU/hwsched、GPU fault/hang 等适配问题，优先从 [bug_reports 源码入口](../../../bug_reports/AGENTS.md) 查对应 ROM 仓库。本机完整 vendor kernel 仓库为 `/Users/bytedance/workspace/swan_source/oak_vendor/vendor/qcom/opensource/graphics-kernel`；参考入口记录的初始 commit 为 `cd8a361e34bbd7e0bbe0eac9bdf3098234bec32d`，分析时须重新记录实际 HEAD。源码在按需挂载的大小写敏感卷，目录为空先按 [源码管理技能](../../../bug_reports/skills/swan-rom-source-management/SKILL.md) 运行其 `scripts/ensure_mounted.sh`。这份 ROM 源码优先于公开 KGSL 快照；精确 ABI/结构偏移或目标版本结论仍需比对设备 ROM manifest、`msm_kgsl.ko` Build-ID/SHA 等身份，不能默认本机 HEAD 匹配当前设备。涉及 Turnip 修改时按用户授权用 `gh` fork 到 `tencentmalos`，在独立 fork 保存、推送相关修改；不把私有 ROM 源码上传到公开 fork。
- 性能定位优先同一运行的 Litep PROF 与 KGSL/sched ftrace；长 wait 的时间重叠不是持锁或因果证据。条件变量需区分 enqueue→notify、notify→resume、resume→reacquire，并保留对象、owner、调用方和截断边界。
- KGSL 采集入口：[swan-kgsl-debugger](../../../bug_reports/skills/swan-kgsl-debugger/SKILL.md)（本机绝对路径 `/Users/bytedance/workspace/bug_reports/skills/swan-kgsl-debugger/SKILL.md`）。使用独立 tracefs instance、总内存限额、先冻结再收集并删除 instance；禁止把总预算直接写入每 CPU 的 `buffer_size_kb`。Thor 的事件集与 Swan 不同，先读 `available_events`，不能直接套用 Gen8 必选事件。
- 设备权限入口：[AYN Root](../../../devices/skills/ayn-root/SKILL.md)（`/Users/bytedance/workspace/devices/skills/ayn-root/SKILL.md`）；公共命令 `/Users/bytedance/workspace/devices/tools/android-root/rootctl --serial SERIAL probe`，核对型号/build/backend 后再执行已授权诊断。Thor 用 PServerBinder/pservice 临时桥接，普通 adbd 仍是 shell；不要假设 adb root 成功。Swan 对应 `devices/skills/swan-root/SKILL.md`。
- Litep 实现位于 `/Users/bytedance/workspace/spatial_mcp_publish/dev_tools/mcp/litep`。KGSL sidecar 必须绑定源文件 SHA、设备 boot、目标 PID/启动身份、时钟及校准证据，并保留丢事件/overrun/不完整 batch。工具与实测见 [血源 CPU/GPU/长帧报告](docs/validation/android-native-host/bloodborne-cpu-gpu-sidecar-20260919.md)。Litep 本地 bottleneck3 / SDK e00321e：`query_indexed_frames` → `analyze_frame_contributions` 使用完整 source 帧和连续邻帧 exclusive 差异；`load_kgsl_sidecar` 后区分 on-CPU/runnable/sleep，`query_async_flows(order=queue_latency)` 保留显式 Post→执行。`analyze_serialization` 仅输出相关候选，不证明持锁。PID20429 的帧2884：286.17ms中等待239.85ms，通知来自Guest-1；其间生产者on-CPU124.07ms/runnable30.56ms/sleep85.16ms，进一步等待Guest-56。epoll约30Hz，无全局锁串行化证据；血源性能未修复，未做同场景提速声明。86工具测试/82引擎测试通过；11 wrappers 中独立webaccess fetch失败，其余10通过。未推送，保留其它修改。

- **Android menu pruning (2026-09-19):** [audit and boundaries](docs/validation/android-native-host/android-menu-prune-20260919.md). Android Settings uses a separate consumed-only catalog: Internal Scale (default0.5) and guest Shading Quality (defaultHigh/1×1/FDMoff). Remove CPU/Box64, RAW/import/export, legacy drivers/setup/recovery routes, unused controller fine-tuning/touch vibration, external runtime download. Preserve persisted legacy fields and previous memory diagnostics. Buttons/HAT only capture shares native acceptance; inherited graphics/controller/touch values reflect global state. See report for final build/device acceptance; no full game regression or memory optimization.

- **Internal scale memory diagnosis (2026-09-19, uncommitted):** [measurement and limits](docs/validation/android-native-host/internal-scale-memory-20260919.md). Same-APK TMNT hub 1.0→0.5 cached images 386.85→112.62 MiB, but VMA blocks 2495.53→2497.31 MiB and PSS ~4.32 GiB unchanged. Follow-up allocation-lifetime counters prove ~210.30 MiB live full-size upload sources, steady creation/retirement; +~66 MiB unused VMA block space offsets savings. Allocation churn ~2373 MiB/s is NOT measured transfer bandwidth. No permanent-leak/full-game stability conclusion. Add gpu_memory request/status: renderer-owned on-demand snapshots, no GPU idle waits; label RAM as System RAM. Scratch reuse / lifetime pooling / fused detile-scale still unimplemented. Latest diagnostic APK cbbbe44b / host c54eebb7 / JNI a478ac1c; PID4717/gen1, 0.5/High1×1/FDMoff restored, input/capture/debugger OFF, ringON, GPUtimingOFF. No commit/push/full regression; controller-disconnect investigation deferred.

- **Internal scale / Git delivery (2026-09-19):** [implementation and validation](docs/validation/android-native-host/internal-scale-20260919.md), [dependency delivery](docs/validation/android-native-host/git-delivery-20260919.md). Android default 0.5; Settings → Runtime → GPU → Internal Scale supports 0.5/0.75/1.0, restart required, per-game override. Guest shading remains 1×1, FDM OFF. Physical eligible image/RT scaling, BC half-mip drop and Foundation Vulkan ASTC4×4/6×6 encoding; unsafe uses promote native. GPU probes Qualcomm/Turnip each 1,563,042/0; latest config/settings23/0 and APK build pass. 0.75 short gameplay reviewed; later 0.5 manifests timeout/stale/stopped remain unverified, 1.0 comparison and total-RAM savings not established. Controller disconnected/freeze/no-audio investigation deferred at user request; no restart for final default-only APK installation. All existing child source changes saved on owned branches before parent pins; independent dear_imgui is clean and not imported. No full regression.

## 新游戏接入：全量盘点，按功能族批量闭环（用户明确要求，2026-09-17）

- **禁止“运行一次 → 遇到一个 Unsupported import → 只补该入口 → 再运行”的常规推进方式。** 加载阶段已知的 `UNSUPPORTED_IMPORT` 必须整体处理；首个实际调用失败只决定优先级，不代表本轮全部工作。
- 首次接入一个游戏时，先盘点主 ELF 及实际加载的所有 SPRX：记录完整 NID、库/模块、可读函数名、实际绑定结果（guest LLE / 已准入 HLE / 未接入），对未接入项按功能族分类。不能把“源码搜到了 NID”当成实际绑定成功，也不能只读 UI 截断的前几条错误。
- **先对照当前桌面版的整条流程。** 一并检查启动准备、配置/离线策略、sysmodule/provider 准入、对象创建/查询/销毁、输出结构、回调和取消。优先提取共用实现，避免在 Android 重写桌面已有逻辑。PlayGo 的安装元数据、奖杯的 npbind/文件准备、HTTP 的离线完成与清理，均属于各自功能族的一部分。
- 对同一依赖链的离线及通用本地接口，按功能族批量补齐后再设备验证；同时检查同库的相关导入、别名和库后缀，不能等下一次运行逐个暴露。涉及 guest 指针、回调、TLS、句柄或等待的接口必须建立正确的 guest/host 桥接，不能直接调用带 guest 地址的 native 函数。
- 保持桌面的离线/未登录/不可用语义；**不得为了消除列表而统一返回 0、伪造在线成功、空实现存档或吞掉错误。** 本阶段无须扩展真实网络/SSL。明确不在当前离线流程内的在线能力可保留具名拒绝，但必须在清单中说明依赖及不阻塞主流程的依据，不能宣称整个库已完成。
- 每批运行前检查剩余清单和相关族的闭环；完成后归档更新后的覆盖情况与实际新边界。按改动做有意义的针对性检查，遵循用户“暂不做完整回归”的要求；一两个入口通过、发生翻页或进程仍活着，不等于游戏可操作。

- **Bloodborne / Turnip current boundary (2026-09-17, local): [delivery and evidence](docs/validation/android-native-host/bloodborne-adaptation-2026-09-17.md).** Actual Linker inventory861 rows,141 unique refused after SaveData family; auxiliary Rect/Quad VS-export interface12/0, save-memory/search/icon172/0. Qualcomm exact-RDC original/no-FTZ/original proves broken FP32 DenormFlushToZero: narrow driver profile quirk restores title, not all gray/menu rendering. APK87210053 / host50acf7b8, host/JNI RelWithDebInfo. User now selects Turnip: same APK PID1142/gen1/UUIDd39662b1752504fd79d1bf85148dca4b, Mesa26.0.0-devel5ac41be677, reaches real Online/Offline menu30FPS, opening cutscene16–17FPS and character creation10–11FPS with visible model/text. Name entry currently blocked by sysmodule0x96/libSceImeDialog0x805a10ff: no provider, not an actual Unsupported HLE call/fault. Next whole family is checked IME Init/Status/Result/Abort/Term + Android text UI + session cancel, not fake Load success. No playable/save-loop acceptance. Qualcomm capture in previous PID22298/gen2 hung in GPU waits after EndCapture; evidence retained, force-stop required, no valid new gray RDC. Final Turnip ordinary process left at character creation, capture absent/TracerPid0/inputOFF; no full regression/new spec/commit/push. Preserve all previous dirty work and full-family policy above.

- **Runtime VM gate removal — final validation (2026-09-17, local): [implementation and exact evidence](docs/validation/android-native-host/vm-lock-removal-2026-09-17.md).** Impl::vm_mutex / VmGuard / shared vm_token and all HLE VM-mutex parameters deleted. Data copies/I/O run outside metadata synchronization; batch pins carry mapping identities. PrepareMapping drains affected ranges BEFORE the MemoryManager interval-table lock, including multi-alias direct free; unrelated GPU/address queries proceed. New Run execution leases no longer reject ordinary non-X data retirement. Old-order real-MM counterexample10/1expected; repaired40/0. Final Android range167+MM40+AJM130+mutex495=832/0; TSan range172/0; hostcontract46/47+1RWXskip. Earlier nine-suite2647/0 belongs to previous APK. Final APKfbe32126 / host49c9d7c6 / JNIcef0fdec (host/JNI RelWithDebInfo), PID10690/gen1 actual rooftop movement/camera MOVE→ATTACK GAMEPLAY_REVIEWED; UI Stop Stopped/user_stop, TracerPid0. Earlier APK20768a09 trace44.392M/0skipped shows29.685s loading with no VM overlap; final APK still long loading/~12–13FPS, no speedup claim. Private metadata/mapping-writer ordering remains; only explicit CodePublication parks FEX. Not a fully parallel/lock-free VM or multi-VMA rollback claim. RingON/captureOFF/inputOFF/properties0. No FEX/Foundation edits/full regression/spec/commit/push this batch. Supersedes the preceding FIOS entry's vm_mutex-still-exists state; preserve other dirty work.

- **FIOS I/O / VM de-serialization (2026-09-17, local): [delivery/evidence](docs/validation/android-native-host/fios-io-global-locks-2026-09-17.md), [VM-lock removal design](docs/guest-memory-concurrency.md).** GuestStorage now uses stable descriptor leases, per-file cursor/per-save quota locks; native positioned reads overlap, readonly opens share namespace admission. DispatchStorage releases VM gate before I/O and drops pins before errno publication. New MapFreshData/ProtectData lets additive non-executable allocations proceed with live execution/unrelated pins, no owner pause/GPU drain/FEX flush; stacks/TLS/sync objects/Av scratch/eligible guest maps connected. Runtime vm_mutex STILL EXISTS: full deletion requires atomic batch/range leases and moving object state to its own owner, not renaming the gate. Native file363/0, data129/0; host data133/0 + LLVM TSan133/0; old contract46/47+1SKIP. Same-file warm native replay1004reads/17.5MB: raw2.247ms, storage2.317ms, checkedABI2.673ms, no FEX/FIOS benchmark claim. Final APK84be94ba/host66816766/JNI32b20098, system Qualcomm, host RelWithDebInfo. Final120s46.096M events/0skipped: longest25.202s guest-flip gap contains NO VM transaction, while12,373reads/284MB complete (syscall132ms); loading NOT fixed, do not blame all delay on VM or prematurely complete FMOD. Actual Stop/user_stop then normal PID7161/gen2 rooftopMOVE~12FPS; warmup remains unverified/input OFF/profile_io+startup OFF/ring ON/TracerPid0. Design targets deleting all shared VM gate parameters and Write memcpy under global space lock, retaining only private metadata and explicit code-publication coordination. No full regression/child edits/spec/commit/push; preserve dirty work.


- **Native mutex / guest fast-path / shared-lock audit (2026-09-17, local): [implementation, measurements and limits](docs/validation/android-native-host/native-mutex-gil-audit-2026-09-17.md).** Production mutex Lock/Unlock now use stable atomic object lookup and per-object Bionic-backed guard/CV; condition uses selected-waiter notification, cancellation/lifetime races covered. Native494/0, condition67/0 and diagnostic92/0, services73/0, rwlock60/0; LLVM TSan494+92 clean. Shared-contention microbench improves34–38%, independent objects do not improve; wrapper still7–10x raw host mutex in noisy single-thread comparison before FEX costs. Current real-FEX compiled guest13/0:10k atomic pairs0HLE/0.400ms vs same work+20k crossings22.245ms; prototype only, NOT production Orbis ABI. Production guest fast path must unify owner/cond/retirement/VM protocol; do not mix guest CAS with host owner/depth or spin instead of blocking. Additional confirmed wide lock: non-save DispatchStorage holds vm_mutex across I/O, plus GuestStorage global file lock; not fixed here. HLE dispatch/JIT not held under one global execution lock; VM/owner/domain short locks remain. APK00d980fc/hostbb6ca1a6/JNIc173ed47, system Qualcomm, RelWithDebInfo; A/B120s both roof+normal Stop, main sce mutex elapsed12.243→8.379s but cond/usleep waits remain; not matched-phase performance/FPS/loading fix. Old APK pre-install restart SIGSEGV retained. No full regression, child edits/spec/commit/push; preserve prior dirty work.

- **Loading wait-chain localization (2026-09-17, local): [evidence and safe optimization boundary](docs/validation/android-native-host/loading-wait-chain-2026-09-17.md).** Opt-in profile_sync adds checked HLE caller/owner and condition enqueue/selected-notify/resume/reacquire diagnostics; default OFF, no wait semantics changed. Android condition OFF67/0, ON92/0; APK7345216b/host0296f0d7/JNIfa728c03. Exact TMNT loadBankFile → flushCommands path polls 1ms; PCM host enqueue is NOT Studio command/bank completion. Loading main condition9.759s:9.676s before notification,27ms wake/domain-guard +56ms guest mutex reacquire. FIOS FHReadSync and guest zlib job worker located; shared broadcast does NOT prove request-level causality, inflate CPU time unmeasured. Two exact game heap wrappers discard MspaceMallocStatsFast output while incurring another heap lock pair: candidate only, no replacement/speedup claim. 120s capture50.836M/zero skipped; specialized reducer used beyond installed50M stage limit, boundary scopes retained. Added9 semantic symbols/indexrev6, exact-SHA dry-run66 symbols passes. Diagnostic gen1 rooftop/actual Stop; properties0, normal gen2 rooftop MOVE, inputOFF/ringON/captureOFF/TracerPid0/system Qualcomm. No full regression, child changes/spec/commit/push; preserve prior dirty work.

- **Litep startup/multi-frame stages (2026-09-17, local): [delivery and TMNT evidence](docs/validation/android-native-host/litep-stage-profiling-2026-09-17.md).** SDK rebased onto actual upstream main2159879 (no master); Foundation/MCP both pin local0ef631c. Explicit cold mode activation records before any frame; startup and in-game stages are elapsed intervals, not artificial long frames. Installed Litep stages1 adds exact stage/progress queries and cancellable bounded large-file reduction;58 tests and11 wrapper checks pass. GNM FrameMark is a submission epoch, not presented FPS; new VideoOut counters/first-present bookmarks distinguish them. Real final APK first present18.783s, guest entry0.818s. Main-owner startup cond/usleep5.991s and436,918 mutex pairs3.526s; reviewed31.989s loading cond/usleep16.312s and560,169 pairs4.460s, GNM gate0.885s. Elapsed scopes include waits, not on-CPU/critical-path or guaranteed savings.145s trace49.66M events/zero skipped chunks; open boundary scopes preserved. No mutex/game performance change or full regression. Final APK a2e98d7d, PID8165/gen1 menu, system Qualcomm, ring ON/capture finished/startup property0/input OFF. SDK commit local only, other dirty work preserved.

- **Crash investigation / async retirement repair (2026-09-17, local): [evidence and limits](docs/validation/android-native-host/crash-retirement-repair-2026-09-17.md).** Android reusable timeline now requires BOTH GPU completion and the corresponding host vkQueueSubmit return. Explicit waits are cancellable/poison-aware; no queue/device-idle or global recording lock. Real GPU delayed-ack regression: old fog-fixed host 348 checks/6 failures, fixed 348/0; GPU async38/0. ResourcePool searches refreshed tick rather than a captured stale value. FEX adapter adds bounded fatal-only per-PID/generation log (no normal-run sampling/unwind); real-JIT subprocess11/0, fatal SIGSEGV forwarding preserved. APK493e38ad/host73e7ca4c/JNIa8ce07b0, system Qualcomm, host/JNI RelWithDebInfo; PID19740 generations1–3 reach rooftop with startup Cross input, first2 Stop cleanly, third left running/auto input off. Empty fatal logs; no new APK crash in this bounded run. Historical Present fault0xb is NOT conclusively attributed to this race; Guest JIT and HWUI crashes remain unresolved. Native debugger attach failed in host LLDB, evidence exported/session cleaned/TracerPid0. Preserve prior fog repair and dirty work; no FEX-child/Foundation/driver edit, full regression/spec/commit/push.

- **Viewport depth/fog repair (2026-09-17, local): [implementation and evidence](docs/validation/android-native-host/viewport-depth-fix-2026-09-17.md).** Real guest zscale1/zoffset0/MinusWToW was incorrectly converted by clamping viewport endpoints [-1,1] to [0,1]. Generic fallback preserves affine Z in final vertex outputs, original near/far via free clip distances, guest clamp bounds and rect/quad transfer; cache binary7/meta6-or7/key4. Qualcomm separate Position/ClipDistance outputs fail clipping in a valid GPU probe; one gl_PerVertex block passes, without FS discard/waits. Host math14588/0; Qualcomm+Turnip GPU1792/0 each; SPIR-V18/18. Final SYSTEM APK1176585c/host096afb0e, ordinary rooftop PID11724/gen1, actual movement/camera response, hard-edge fog visibly repaired in observed views; GAMEPLAY_REVIEWED, ~12FPS (no speedup claim). Frame12 still MOVE, not ATTACK; corrected review annotation. Prior Present driver, intermediate Guest JIT and HWUI crashes retained/unresolved, no ten-minute/full-game stability claim. Fallback clip-slot/cull/feature restrictions and untested GS/multi-viewport GPU combinations documented. No new RDC/FEX/Foundation/driver/queue changes, full regression, spec or commit/push. Auto input off; game left running, TracerPid0. Preserve previous dirty work.

- **Residual fog / Turnip source / completed post-fix RDC (2026-09-17): [evidence](docs/validation/android-native-host/fog-depth-range-turnip-2026-09-17.md).** New Qualcomm RDC34b56a43 is complete, 1,110,985,693 bytes; same-ICD final replay/depth grid confirms clear1 values498/510→0/510, residual fog remains. Turnip4397 (FSd63cd69e/VSc9f45533, pipeline11803,504 triangles) introduces hard-edge fog. Captured API: negativeOneToOne=1, viewport depth[0,1], LESS/testON/writeOFF. Diagnostic GLSL matches original within4 pixels/1 code value; soft fade is saturated. Exact diagnostic fragment viewZ=-34.12475 vs depth-derived67.47783; inverse half-range depth changes scene to34.917034/soft0.528 and visibly reduces hard edges. Prioritize generic viewport range-preserving compatibility at vk_rasterizer.cpp:1238, NOT a TMNT shader hack or extra queue waits. Guest zscale/zoffset not directly captured; no production fix deployed. Local Turnip source903957fe differs from installed5ac41be677; documents static/dynamic invalidation, FS/LRZ, subpass and viewport formulas. Qualcomm intermediate replay crashes in vkBeginCommandBuffer; do not claim full intermediate analysis. All6 replay shader replacements restored, debug/capture closed, temporary tool source/dylib/settings restored. Final ordinary SYSTEM APK5884b68f at rooftop MOVE, PID24787/gen1, TracerPid0, no RenderDoc; auto input stopped. Main/FEX/Foundation/driver unchanged; no full regression/commit/push.

- **Rooftop rendering claim correction / system-driver recheck (2026-09-17): [updated evidence](docs/validation/android-native-host/turnip-qualcomm-depth-state-2026-09-17.md).** PARTIAL repair only: severe Qualcomm rail/window block artifacts improve, but large triangular/strip-like fog hard edges remain in BOTH the current system APK and prior Turnip RDC output. Turnip is not a fully correct visual baseline. APK5884b68f unchanged, ordinary layer-free system run PID28479/gen1/UUIDb3e2e4f5c7742737240987b82b7319bc reaches rooftop ATTACK and responds to movement; current driver SYSTEM, game running, auto-input stopped, TracerPid0. No new RDC. Host dynamic-cache audit found no obvious invalidation omission in inspected paths; driver internal cache cause remains unproven. Supersedes broad fog/rendering-fixed language below; no further production edit this check.

- **Turnip/Qualcomm rooftop RDC and depth-state repair (2026-09-17, local): [evidence and limits](docs/validation/android-native-host/turnip-qualcomm-depth-state-2026-09-17.md).** User explicitly requested new Turnip capture, superseding earlier no-capture restriction. Normal Turnip rooftop RDC895f7f98 / 1,208,629,838 bytes / flip1411→1412 is Ready and same-ICD replay verified. Old Qualcomm depth-copy510-point grid has498 clear1.0 vs Turnip0; source D32S8 differs too. Full-replay audit rejects apparent1036→1045 depth-loss timeline as split-replay dependent, but final badPNG/grid unchanged. Cross-ICD old capture rejected storageBuffer8BitAccess; no feature bypass. Qualcomm-only post-graphics-bind re-emission of depth-test/write/compare (direct+indirect paths) restores ordinary APK rooftop; all-state diagnostic narrowed to these3 commands. No shader/Int64/FEX/queue/barrier change or FPS claim. Postfix Qualcomm RDC attempt killed by LMK (LOW_MEMORY), incomplete589MiB is NOT replay acceptance. Normal Turnip requested capture remains complete. Temporary RenderDoc source/dylib restored exactly, captures/settings/input cleaned; final driver Turnip. No full regression/commit/push; preserve previous dirty work.

- **TMNT Qualcomm rendering investigation (2026-09-16, local): [evidence and negative experiments](docs/validation/android-native-host/qualcomm-rendering-debug-2026-09-16.md).** Rendering remains unresolved. Existing rooftop RDC only, no new capture. System-driver 8/16/32/64-bpp tiler+detiler checks 8/8 exact1MiB; not all tiling modes/mips. Static vertex input, disabled float-control modes, outgoing attachment barrier, LDS barriers, Position Invariant, and disabling clamp while clipping each leave rooftop artifacts; all experimental production edits withdrawn byte-for-byte. Existing-frame depth8784→R32copy10232 matches sampled values; equivalent copy-shader replacement leaves output PNG identical. Main sampled draws use depth Less/write ON, viewport1920×-1080/Z[0,1], not EQUAL. Sparse depth1 samples are a clue, not proof of missing covered geometry. RenderDoc pixel history crashes replay; zero modifications are invalid evidence. Temporary native pipeline diagnostics and dylib restored; no new MCP release. Latest baseline APK/artifact identities in report, normal UIStop verified, no auto-input/replay/debugger left. Preserve prior dirty work; no full regression/commit/push.

- **Foundation/ring/graphics tooling and existing-frame analysis (2026-09-16, local): [delivery](docs/validation/android-native-host/graphics-tooling-foundation-2026-09-16.md), [analysis](docs/validation/android-native-host/existing-rooftop-gpu-analysis-2026-09-16.md), [async submission](docs/validation/android-native-host/async-submission-2026-09-16.md).** Foundation main7474e30 merged locally asbb52b26, SDK9325b81, prior audio retained; not pushed. Ring realAPK7421 GPU pairs/813 orphan discarded; litep gpuring2 installed, uncalibrated GPU elapsed usable, CPU joins refused; reconnect old MCP. Guest-flip capture/markers and independent writing deadline:65/0, productionScheduler330/0; Ready619MB, olderroofRDC Android replay verified. Roof repeat capture LMK and GRS overflow/token0 remain unresolved. User now says STOP NEW CAPTURES; use existingRDC/PROF. Existing roof has ONE main guest command buffer, 32 confirmed hostdetilers, not32 CPU waits. Replay533 GPU durations: detile11.492ms/copy6.476ms; cannot equate replay action sums with actual64.581ms workingbatch. 28.266ms outside render-pass scopes is UNATTRIBUTED, not proven barrier/idle. Exact candidates events736–764 guestcompute4.710ms and785–823 five detile+copy6.329ms; resource lineage in report. 4 present-type nodes include syntheticEndCapture, only3 actual queuepresents. Asyncworker alreadyexists; label42.9ms waits throughPresent, no earlyrelease. APK still2126d946 playstoreDebug, host/JNI RelWithDebInfo, FEXRelease; marker-only rebuild7c2f47ce not installed. UIStop0.81s, capture/GRS settings restored, replayclosed. No new performancefix/fullregression/maincommit/push; preserve dirty work.

- **Qualcomm Int64 compatibility and rooftop profiling (2026-09-16, local): [implementation](docs/validation/android-native-host/shader-int64-system-driver-2026-09-16.md), [performance/Citron comparison](docs/validation/android-native-host/qualcomm-performance-2026-09-16.md).** System shaderInt64=0 now uses verified U64 u32-pair lowering including BDA, shifts/comparisons/rounding; atomic64 unsupported is rejected. Probe system13059/0, Turnip pair+native24580/0; SPIR-V/build pass. Final APK8ecd6059/host5cae6686, PID10213/gen1/UUID26e9c15d50b45de8cdc5c875729cbb82 reaches rooftop ATTACK; final warmup TIMEOUT_UNVERIFIED retained, pixel corruption remains, no more image A/B per user. Same-system profile: GPU guest61.8ms, CPU frame68.8ms; real submit0.061ms, GpuComm queue-lock19ms/call because Presenter holds the same queue lock in blocking present. Dedicated PresentThread ALREADY exists; Citron additionally uses CommandChunk/VulkanWorker, while retaining submit/present synchronization. Do not merely add another present thread or delete the queue lock. GPU render-pass coverage33.56/61.87ms; largest uncovered gap5.34ms before zero-based pass22 is not yet a semantic shader identity. No performance-threading fix or30FPS claim. First AvPlayer crash retained/unresolved. Preserve previous dirty work; no child edits/full regression/commit. Final system game running, ring+coarse queries ON, overlay shown, auto input/file capture/debugger OFF.

- **Frame-tail wait / reusable interception workflow (2026-09-16, local): [diagnosis](docs/validation/android-native-host/frame-tail-wait-2026-09-16.md), [end-to-end workflow](docs/guest-frame-interception-workflow.md).** Shared and installed guest-auto-tag skill now documents capture → semantic catalog → frame workset → editable C++ → exact-build hit feedback, retaining CPU adapter/ABI and overhead limits. Screenshot frame43: SubmitFlip19.524ms contains GNM gate19.246ms; GpuComm queue-lock19.244ms overlaps Presenter submit19.217ms. Dispatch14.305ms is command generation (HLE4.049ms), not another proved GPU wait; +0x3da80 resolves to sceGnmSubmitDone. Same Thor/current Turnip SHA fdd37852 standalone HAL probe proves KGSL timeout0 blocks: QueueSubmit A/B/A66.827/0.039/66.754ms, zero-wait66.782/0.062/66.625ms; B uses real RETIRED timestamp queries and retains eventual completion. Exact driver FP chain reaches timeline GC→kgsl_syncobj_wait→wait_timestamp_safe; no APK/game A/B yet, no claimed game speedup. New standalone diagnostic never links its ioctl interposer into production. No production/FEX/Foundation/driver replacement, full regression, spec or commit; device kept in Settings, no active game/debug/capture/input. Prior dirty work preserved.

- **Visible frame → C++ interception (2026-09-16, local): [delivery](docs/validation/android-native-host/frame-interception-2026-09-16.md), [editable sources](guest/games/CUSA50828/01.08/intercepts/README.md).** 50 resolved frame entries all map to buildable C++:44 new semantic-named register-preserving entry observers +6 existing entries, combined with4 helpers=54hooks. Opaque x86-64 SysV/AVX entry adapter preserves GPR/flags/original stack+return/x87/MXCSR/YMM; callback then original tailcall, NOT body recompilation/exit interception. Per-hook VM-token toggles and exact build/source/runtime hit refresh; source edits invalidate old evidence. RealFEX27/27 uncovered and fixed child FXRSTOR nonzero-TOP physical-register rotation; other existing child dirty work preserved. Generator8/builder11/status6/reader30; ordinary APK PID8776/gen1/context1 exact package7cafaa22 installed54,44/50 visible functions actually hit,6 not hit. Warmup stops after foreground switches to WLAN settings/service disappears; NO new rooftop or perf acceptance. reverse_study intercept1 installed, actual MCP source-link flow and11 wrappers pass. Next startup restored v2 light package; no auto-input/capture/debugger. No full regression/commit/push.

- **Measured frame recompilation / semantic symbols (2026-09-16, local): [delivery](docs/validation/android-native-host/frame-recompilation-2026-09-16.md), [source entry](guest/games/CUSA50828/01.08/README.md).** User requires actual frame call order and C/C++ main bodies, not only top-three waits/helpers. 384 probes include341 FrameCoordinator callsites; gen1 retained197frames/42direct/20indirect sites, v2 gen2 retained200/49direct/23indirect. Canonical symbols/index.json v2 revision5 has57 evidence-backed semantic names, including root+49direct targets, helpers and inner semaphore/TLS waits; candidates remain candidates. Frame views/study0/source filenames/next plans share the index, offsets remain identity. Bulk native export removes64KiB pseudocode/128KiB body/1000CFG truncations, includes lvar register/stack and early/final IR, separates original-input/database SHA; 50 full source functions, inferred not compiled. New frame_dispatch.cpp implements DispatchFrameListsAndRecycleBlocks beyond old4helpers, real-FEX original-code differential16scenarios/17checks and ordinary APK rooftop MOVE→ATTACK; counter15 proves2304calls. FrameCoordinator still original timing wrapper; whole-frame C++ incomplete. Page/mapping probe preimage fix8/8, existing auto48/48, reader29, contract8. reverse_study frame2 installed, Codex/wrappers11 verified; saved DB regenerates identical384 machine probes with names. No speedup claim/384probe-overhead A/B/A/full regression/FEX-or-Foundation child edits/commit. Final gen3 unarmed reaches rooftop but foreground changes to Android Settings and normalCancelled; preserve failed restore manifests, do not claim final warmup pass. No debugger/capture/automatic input; next-start profile cleared. Preserve previous dirty work.

- **TMNT selective recompilation (2026-09-16, local): [delivery](docs/validation/android-native-host/guest-recompilation-2026-09-16.md), [workflow](docs/guest-selective-recompilation.md).** New `guest/games/CUSA50828/01.08/rooftop_recompiled.cpp` implements four real guest functions (label writer, default-state writer, submit arrays, nine-argument label+flip), beyond the old five timing wrappers. Same-module typed function/data bindings preserve original storage, checked preimages/permissions/lifetime, no SDK crossings for direct bindings. Real original-machine-code FEX differential40cases/41checks; bindings18/18, loader41/41, builder11/11, contract8/8. Actual APK2412969b/host189a1b29, PID9049/gen1/context1 on Thor/Turnip reaches rooftop MOVE→ATTACK, GAMEPLAY_REVIEWED. A/B/A12.986/13.002/12.939FPS, no speedup claim; final199frames mean76.637ms, main cost still GNM gate. Shared reverse_study recompile2 tool/skills installed, complete SDK code/data evidence, AArch64 static only, wrappers11/11; typed symbol index9symbols/2types validated. Contract/source hashes and original/recompiled effects retained. Guest package562938ee remains enabled; no auto probes/debugger/capture/input, ring/GPU on. No FEX/Foundation edits, full regression, spec, commit/push; preserve earlier dirty work.

- **Guest auto tag / reverse_study (2026-09-16, local): [delivery](docs/validation/android-native-host/guest-auto-tag-2026-09-16.md), [workflow](docs/guest-auto-tag.md).** User now explicitly enables auto tag; old deferral is superseded. Public MCP `reverse_study`, backend `study0` default, `study1` unavailable. Default depth2, top3 measured roots, maxProbes0=no user cap (NOT64); capacity1024 probes/64 expanded functions refuses without silent truncation. Stepwise targetOffsets/depth1–4, next request is a suggestion only. FEX immutable opt-in IR probes preserve guest state, owner/invocation+RSP pairing, epochs/stale-code rejection, C/C++ trampoline relocation. Native48/48, batch15/15, helper3/3, real AArch64 static11 checks, wrappers11/11. Actual TMNT134 probes/237sites with existing rooftop C++ package, ordinary APK PID27493/gen2/context33, MOVE→ATTACK. Live15s204frames/165long, 318969 complete probe pairs; no overflow/invalidations; boundary incompleteness retained, no lossless claim. Auto call+1545f69→+19630 aligns with SubmitAndFlip/SubmissionGate waits; downstream GPU causality unresolved. OFF/ON/OFF13.365/13.502/13.381FPS, not zero-cost proof; unarmed vs armed-disabled differ. Skills and local release installed in spatial_mcp_publish. Final gen3 restored unarmed/13.373FPS, input and capture stopped, TracerPid0; restore timeout manifests retained with separate visual review. No commit/push/full regression; prior dirty work preserved.

# Repository context for coding agents

- **Vulkan wait-chain / CPU and shader audit (2026-09-16, no production edits): [evidence and configuration](docs/validation/android-native-host/vulkan-wait-chain-2026-09-16.md).** Thor PID8463/gen2: exact 2.019s handoff+sched window shows GpuComm Submit803.55ms with only2.51ms on-CPU, mostly awakened by GpuDone; queue-lock waits align with Presenter ownership, guest wakes from GpuComm. Overlay A/B/A13.363/13.618/13.404FPS despite submits129/36/129 per second. Pinned Mesa timeline GC zero-timeout KGSL poll plus reference-kernel zero=infinite semantics is a concrete candidate, NOT proven for the running kernel; user defers deeper work until a root device. FEX is optimized JIT with unconditional GDBSERVER1/MULTIBLOCK0, scalar TSO on and x87 reduced precision off; shaders use NoContraction, no Citron-style accuracy preset. No lock removal or precision changes. Native attach failed and cleaned/TracerPid0; raw2s trace no reported overrun, first10s trace and Perfetto probes rejected. Warmup stale-review ERROR retained despite actual rooftop movement images. Capture/input off, overlay restored, game running; preserve all previous dirty work.


- **TMNT rooftop function calibration / Android face mapping (2026-09-16, uncommitted): [delivery and limits](docs/validation/android-native-host/tmnt-rooftop-calibration-2026-09-16.md).** Exact-SHA eboot recipe adds FrameCoordinator +0x9e560, BeginFrame +0x1545ce0, SubmitFlip +0x1545f20, JobBarrier +0x1591570 and FMOD-update PLT +0x15c0b20; 5 typed reconstructed symbols validated, correlated not vendor symbols. APK9c28a1b9/unchanged host8bf15985/JNI57cfd575, PID8463/gen1/context1 reaches rooftop MOVE→ATTACK, real movement/camera, GAMEPLAY_REVIEWED. Unattached35s PROF:442 interior frames mean78.677ms (pre-render7.459/begin1.295/build22.255/submit47.645), FMOD0.095ms; jobs7.553ms already included. Submit nearly all GNM.SubmissionGate; GpuComm and Presenter show long shared queue mutex + queue.submit, not proven shader-only. GPU GuestFrame67.329ms incl GPU waits, estimated CPU alignment±5.811ms. Main patch ON/OFF/ON12.782/12.804/12.735FPS; SDK stops while disabled. Retain boundary-truncated CPU diagnostics, no global lossless or performance-fix claim. Final user mapping default A=Circle B=Cross X=Triangle Y=Square; default-off Xbox/Nintendo flip swaps pairs, app-level profile applies before Orbis, touchscreen/Foundation unchanged. Unit95+24/0, ordinary input APK7/0 with synthetic device-labelled KeyEvents, not physical-human acceptance. No full regression, host/FEX/Foundation changes, spec or commit this batch. Measurement session normally Stopped/user_stop, no debugger; preserve previous dirty work.


- **Guest function patch / custom SDK v1 / TMNT calibration (2026-09-15, uncommitted): [guide](docs/guest-function-patches.md), [delivery/evidence](docs/validation/android-native-host/guest-function-patch-2026-09-15.md).** Production startup loader now installs exact title/module/optional executable-SHA-bound x86-64 function-entry patches. C/C++/ASM freestanding guest SDK, typed original imports, RX/RW data/BSS/internal pointer rebasing, Zydis whole-instruction/RIP/CALL/Jcc trampolines, session SDK clock/counters and litep export are implemented. Install requires zero guest handles; online enable/disable changes resident slots under the normal VM token, never frees in-flight code/state. Actual post-copy invalidation failure blocks old JIT via poison. Fixed production CPU/GPU composite-mask permission bug that made read-only pages writable. Real FEX41/0; builder7/7; ordinary APK PID31985 three generations ×10k calls. Final APK eada9e30 / host8bf15985 / JNI57cfd575; TMNT package bbe17a87 on PID32104/gen2/context33 reaches rooftop and actual movement/camera response, GAMEPLAY_REVIEWED. Update1/16 sample:24 calls avg85.195us/max197.292us, results0; flush hooks installed but NOT dynamically hit. Disable15.647s SDK770→770/flips+207; enable resumes; stale context refused. Final PROF657 chunks/0skipped with CPU boundary diagnostics retained, no globally lossless or performance-fix claim. FEX/Foundation/Oboe unchanged this batch; no auto tag, production mutex replacement, full regression, Swan, main commit/push. Game left running on Turnip, patch/ring/coarse GPU enabled, auto-input/file capture off, TracerPid0. Preserve previous dirty work.

- **GPU async / litep timequery / system-driver comparison (2026-09-15): [implementation, evidence and limits](docs/validation/android-native-host/gpu-timequery-2026-09-15.md).** Android completion worker removes blocking timeline counter reads from producer paths; IRQ one-shot callbacks release subscriber lock, Presenter holds last-frame ownership through redraw. Native async22/0; GPU timing27/0; real Turnip query collector26/0. SDK368177d retains GPU clock metadata; macOS/Android wire5/5+clock4/4, Android encoding2/2. Actual APK2ddba5ca/host6e6b8a2f, PID17372/gen1/UUIDdc3add49f59e4b67716301959ec74d43 reaches rooftop MOVE→ATTACK, warmup GAMEPLAY_REVIEWED; three captures~13.1FPS, complete GuestFrame65–66ms, PP~0.38ms, Present~0.58ms. GPU device elapsed includes GPU waits, not shader busy; no30FPS claim. Detail8595render-pass intervals; detail/restored capture three GPU lanes and no GPU pairing/range errors. First coarse capture lacks two context definitions: retained failure, not complete GPU capture; CPU boundary diagnostics retained in all. Actual Turnip has no calibrated-timestamp extension: estimated CPU alignment±8.34ms. Explicit system-driver APK really loads Qualcomm Adreno740 but rejects shaderInt64=0 at VideoOutOpen; no system rendered/performance comparison, Turnip restored. SDK metadata, standalone profiler_ring CMake and guide published: Foundation main e8fc031 / owned branch d41ade4, SDK owned branch368177d. See [Foundation guide](foundation/docs/guides/litep-gpu-timequery.md). Main production edits remain uncommitted; Foundation audio remains dirty. One intermediate Guest JIT SIGSEGV is unresolved, no stability/full-regression/Swan acceptance. Game left running, automatic input off, ring/coarse GPU on, file capture off, no debugger. Preserve earlier work.


- **CPU boundary optimization / GPU query check (2026-09-15, uncommitted): [implementation and gameplay measurements](docs/validation/android-native-host/cpu-boundary-optimization-2026-09-15.md).** GuestAddressSpace now caches 64 mapping indices, rechecking live range/permission under the existing lock; no copied permissions/pointers/negative results or mapping reorder. Mutex owner/count uses one checked12-byte write. FEX adapter keeps refcounted frame registration per physical Run and owner-local fault attribution; nested callbacks/cancel/debug semantics retained. Native API47/47, services73/0, condition66/0; real-FEX HLE43/0, publication6/0, debugger85/0, compiled guest prototype13/0. 2056-map host microbench A/B/A:200k queries399→7→413ms;20k mutex pairs252→8→256ms, not game speedup. APKb4c92212 / host641421e8 / JNI4e5c8288 installed; PID25383/gen1/UUIDf88b351e80ff7e34439f4e0272f1ab71 reaches actual rooftop and MOVE→ATTACK/input/camera response; warmup GAMEPLAY_REVIEWED. 45s simpleperf5747/lost0: lookup leaf12.34→2.84%, VM stack21.57→9.33%; gameplay remains12.415FPS and Prepare66.624ms, GPU615/680MHz, no FPS-gain claim. Mutex HLE frequency rises to~88.5k/s; this batch reduces per-call cost, NOT crossings. Production compiled-guest mutex is still NOT enabled; unified ownership/lifetime/cond protocol is required. GPU timestamps are NOT connected to Android ring/StatusLayer: common/debug.h hardcodes TRACY_GPU_ENABLED0 even for current desktop defaults; retained Tracy code is not evidence of active queries. No Vulkan/GPU query implementation, FEX-child/Foundation/Oboe changes, full regression, spec, commit/push this batch. Game left running, automatic input off, ring on, capture off, no debugger attached. Preserve previous dirty work.

- **Actual TMNT gameplay warmup and performance (2026-09-15, uncommitted): [measurements and limits](docs/validation/android-native-host/gameplay-performance-warmup-2026-09-15.md).** Existing HTTP2 APK37330cd7 / host0f5649b4 now verified through PLAY/tutorial loading into Leonardo rooftop, movement/camera response and MOVE→ATTACK. Default Cross confirms; Circle reaches real guest Pad but cancels under current system mapping. Added external `scripts/android/warmup-game`, explicit visual/input acceptance and 7 targeted tests; movement-final exits0 GAMEPLAY_REVIEWED, later restored-run times out unreviewed and remains non-pass. PID410/gen2 gameplay profiles: ~12.7FPS/79ms, VideoOut.Prepare ~66.5ms; frame-pool/fence/queue-submit tagged waits tiny. Device GPU busy~87% at680MHz, sampled GpuComm adreno_drawctxt_wait, specific pass/host subcall unresolved. User-space simpleperf6393/lost0: ValidateRangeLocked leaf12.34%, VM21.57%, FEX host adapter17.30%; ~57.5k HLE mutex lock/unlock calls/s. Audio producer QueueWait is backpressure, not main-thread output CPU time. PROF17/11 gap markers occur before each affected thread's first event; gap-aware pairing leaves metrics unchanged, no claim of globally lossless capture. Post-profile native attach failed/timed out and perturbed gen2 continuation; cleaned/TracerPid0, game restored to gen3. Ring ON/OFF/ON12.625/12.649/12.743FPS; ring ON restored, automatic input stopped. No performance fix, full playability/loading/ten-minute/Swan acceptance, production/child edits, full regression, new spec or commit this batch. Preserve prior dirty work. Next prioritize precise Prepare/Flush + GPU-pass attribution, then correct VM lookup/compiled-guest mutex/HLE entry optimization; do not restart Oboe or claim all audio cheap.

- **HTTP2 offline call-chain repair (2026-09-15, uncommitted): [implementation and device evidence](docs/validation/android-native-host/http2-offline-repair-2026-09-15.md).** Desktop HTTP2 async send/read/wait now reject unavailable work instead of returning fake OK; HTTP1 offline worker was the semantic reference, not a complete HTTP2 implementation. Production owns 25 offline HTTP2 NIDs with bounded context/template/request handles, checked guest buffers, negative send results and normal deletion; no async success event, network or TLS capability. The exact audited executable code SHA gates restoration of 21 baked zero-return PLT slots to normal GOT imports in load memory only, under the existing VM publication token; on-disk SELF stays6122da71. Final native240/0. Final APK37330cd7 / host0f5649b4 / JNI71751473: PID410/gen1/UUID340f0cac9b86fab7e1febe345b102801, unmodified source content, no debugger; real requests6/7/8 reach HLE, fail send and are deleted; menu renders at180–220s ~37FPS, Stop/user_stop, TracerPid0. An earlier Prepare writable-pin mistake is fixed with token publication and a regression check; failure evidence retained. No PLAY/ten-minute/Swan acceptance or FMOD loading-performance fix. FEX/Foundation/mutex/Oboe unchanged this batch, previous dirty work preserved; no new spec/full regression/commit. Supersedes the preceding diagnostic-only HTTP2 status.

- **Guest HTTP2 white-screen root localization (2026-09-15): [exact guest/file evidence](docs/validation/android-native-host/guest-http2-white-debug-2026-09-15.md).** Fresh PKG extraction and live guest reads match 21 baked return-zero HTTP2 slots; sceHttp2WaitAsync never reaches host HLE and returns success without an event. Both video OnComplete actions run and drain; startup index7/auth state3 waits on a request permanently in progress. Normal recorded native-source rebuild also whites before input, so debugger backport/mutex rollback is not an established fix. A temporary exact-SHA six-byte negative-return injection ends/releases auth, clears HTTP queues and reaches real PLAY menu at200/240s. Independent fully-unattached replay D retains the menu at180–310s (~37–52FPS), then Stopped/user_stop with original SHA restored; Circle/Cross injection does not prove entry into gameplay. Before recovery, a distinct FMOD synchronous load/flushCommands wait causes temporary guest0FPS; it later completes, not a proven deadlock. Diagnostic content is automatically restored/sha-checked; no permanent game patch or full playability acceptance. Only production change is bounded terminal-reason logging; current FEX/mutex/audio source restored/preserved. See report for no-debug replay, identities and cleanup limitations. No new spec/full regression/commit.

- **Normal Session comparison / mutex A-B-A (2026-09-15): [audit and device evidence](docs/validation/android-native-host/normal-session-mutex-audit-2026-09-15.md).** Normal PID22870/gen1 main menu58FPS already used Oboe and per-object host mutex wake (hostbadb52cd/APKe6f92f07). Canonical source1110:1099 unchanged/11 changed, mainly guest debugger/FEX; audio19 files identical. Compiled guest mutex remains test-only, production still GuestMutexDomain. Reverting only guest_mutex.h to global CV did not remove white (B~28 flips/s); restored per-object A2 also white~50/s, both actual Stop Stopped/user_stop. B/A2 canonical1114-file manifests differ only mutex header; APK entries differ only host DSO. This does not exclude older mutex/VM protocol bugs or explain original post-PLAY0FPS. Default unattached FEX generated-trap redirection remains an unproven regression candidate. No permanent production change/full regression/spec/commit; earlier dirty work preserved. Final installed A2 APKc4d5d027/hostad41b9a0/JNI5f839d1b, PID22146 stopped/alive/TracerPid0; source restored and verified.

- **Latest mixed-stack / watchpoint implementation (2026-09-15, local): [delivery and constraints](docs/validation/android-native-host/guest-debugger-mixed-watch-2026-09-15.md).** Guest read/write/access software watchpoints and bounded mixed stacks are implemented; initial audit limitations below are historical. Normal unattached execution emits no watch IR and captures no native stacks; only armed watches use uncached instruction stepping. Native77/0 covers same-value/atomic/vector/stack accesses, unsupported preflight, stale mapping, HLE configuration, detach recovery and nested mixed boundaries. Mixed frames preserve context/thread/generation/invocation/provenance: verified RBP links plus historical host HLE/callback/wait captures, not full DWARF or current asynchronous host unwind. REP/masked/gather/non-temporal and unhandled memory forms are refused before execution; host HLE/GPU memory writes are outside guest watch scope. Refusal is a recoverable T05 unsupported stop, not a missing stop/E16. Native MCP adds read_stack and structured WatchHit; exact final APK/MCP results and source hashes are in the delivery artifacts. FEX child now has six-file opt-in IR/pass changes on the owned branch (local, not a published pin); main/previous Foundation/Oboe/audio work preserved. No TMNT zero-FPS fix or performance/playability acceptance, no full regression.


## Project objective and current state

- **Tier B 之后的诊所瓶颈（2026-09-20，Litep PROF+KGSL 同窗）：** [归因报告](docs/validation/android-native-host/tier-b-litep-bottleneck-20260920.md)。快路径 installed、10.3 FPS：GPU busy 85.7%（680 MHz 上限）、主 batch submit→retire p90 80 ms；Guest-1 on-CPU 64.5%（JIT 8.1 s/14.4 s）+ runnable 17.9%；GpuComm PM4 on-CPU 70.7% 且常驻 1.84 GHz 的 cpu7；帧 owner Guest-19 每帧等 Guest-1 74 ms（notify→resume 0.06 ms）。CPU 同步不再是主矛盾；下一杠杆是 GPU 每帧工作量、大核争用/频率、剩余 ~957 次/帧 HLE（约 15 ms）。Litep 已在 Windows 从源码跑通（engine 管道读取、capture_kgsl LF/exec 位/rootctl 前缀修复，`rootctl-win` 包装）并注册为用户级 MCP。工具缺口与 Windows 本地发布 spec：[litep-perf-tooling-gaps-20260920](docs/specs/litep-perf-tooling-gaps-20260920.md)（同窗采集编排、GPU busy/CPU 频率/唤醒并入 bottleneck、每帧 HLE 预算、hle_sync 窗口比较、win-x64 打包）。
- **Orbis 同步 Tier B（2026-09-20，`feature/malos/fex_sync_performance`）：** [实现、单测与实机 A/B/A](docs/validation/android-native-host/fex-sync-tier-b-20260920.md)。随应用发布的 guest x86 mutex 快路径（`guest/runtime/sync/mutex.c`，算法=桌面 libthr `mutex.cpp`，锁字=compiled_guest 三态，host 只提供 `shadSyncWait/Wake` 期望值等待/按地址唤醒），首次 `Bind()` 时发布并接管 `pthread_mutex_lock/trylock/unlock`、`scePthreadMutex*`、`pthread_self/scePthreadSelf` 8 个符号；host `GuestMutexDomain` 改为读写同一 guest 锁字（owner/count/state 在 prefix，host 不再存副本），cond/rwlock/sem/kernel sema 仍 HLE。真机 FEX 快路径测试 29/0，native mutex 65499/0、cond 67/0、services 78/0、rwlock 60/0、kernel sema 588/0。血源诊所同 APK 同进程 A/B/A：同步 HLE 104.6k→12.1k→102.5k 次/s，FPS 7.4→**10.7**→7.2，Guest-1 12 s 唤醒 89k→29k。`debug.shadps4.sync_fastpath=0` 关闭；`hle_sync` 新增 `fast_path` 字段与 `Sync.AddrWait/AddrWake`（A/B/A 的 APK c0a7cd36 尚无该分类；补测 tierB2/B3-on：AddrWait 约 490 次/s、38% 真正停车、Park 占 elapsed 88%，AddrWake 约 1.5k 次/s，10.8 FPS）。无完整游戏回归、无其它标题验证；二期 cond/rwlock/sem_t 未做。
- **Orbis 同步 Tier A（2026-09-20，`feature/malos/fex_sync_performance`）：** [离线评估](docs/validation/android-native-host/orbis-sync-offline-review-20260920.md)、[host 修复与实机 A/B](docs/validation/android-native-host/fex-sync-tier-a-20260920.md)。kernel sema/mutex/cond/sem_t/rwlock 均改为锁外 notify，`Current()` TLS 缓存，WriteData/lease 释放减少锁往返；设备单测全绿，Guard 阶段降到 0.2–0.4 µs，但血源诊所帧率无变化。simpleperf 定位到 `GuestAddressSpace::lock`（每次同步 HLE 7 次）与 FEX context 锁是真正串行点；host 侧上限已到，下一步是随应用发布的 guest 快路径（Tier B）。Windows 主机可完整构建 host/APK（ffmpeg 子仓修复在 `codex/windows-host-build` 88b5e6b8，未推送、gitlink 未推进）。
- **Latest white-screen/media/debugger investigation (2026-09-15, uncommitted): [evidence and repairs](docs/validation/android-native-host/guest-white-media-debug-2026-09-15.md).** Persistent white at 50–60 guest flips/s also reproduces unattached; do not conflate with original post-Circle/PLAY 0 FPS. Both AvPlayers return false (243 video/349 audio, then180 video/0 audio); real guest breakpoint confirms completion getter returns1 and caller dispatch bit already set, so missing EOF/guest completion is not established. Oboe sources2→1/continued consumption is not full FMOD command completion proof. Real Android Turnip RDC event71 samples already-white RGBA texture3794 into601; no producer inside capture, investigate preceding source update/transition consumer. Fixed attached HLE native-unwind overhead via per-Run/per-attachment entry template, and software BP rejection of WaitingHle while preserving pause/lease/pin checks. Device focused debugger85/0; AvPlayer1207/0 after Ready-callback test handshake (1130/7FAIL retained). Actual APK breakpoint hits/clean removal, --open_last_game cold/warm/dumpsys connected. APK445e1ec4, host2eec5bef, JNI5f839d1b; dirty source manifest in report. UI Stop/guest/PROF/RenderDoc/scrcpy cleanup complete; no white-screen/playability fix, no full regression, FEX child unchanged. Preserve existing dirty work.

- **Guest debugger implementation (2026-09-15, uncommitted main work): [delivery, controls and limits](docs/validation/android-native-host/guest-debugger-implementation-2026-09-15.md).** This supersedes the earlier same-day Step Unsupported / no production RSP audit below. The main FEX adapter now provides owner-thread debug parking, epoch-checked register mailbox writes, real TF single-instruction execution, recoverable INT3, checked software breakpoints/cache invalidation and session-owned loopback RSP with loader modules. Guest-stopped snapshots and read-only HLE boundaries are distinct; thread descriptions include host TID, invocation and HLE operation. Never treat HLE Pause as native process suspension or arbitrary JIT RIP reconstruction. Native54/0; ordinary APK three same-PID debugger generations plus disabled-listener/Stop test pass; new independent Native MCP real-FEX attach/read/step/breakpoint/remove/step/cleanup passes. Spatial MCP client PC/default-register assumptions fixed in local dev_tools f6385072 / publish4be28e3b, installed local20260915.fex; 32 tests and full solution build pass, 11 wrappers smoke pass. Existing connections may still run the old MCP binary. FEX child unchanged; preserve prior dirty Foundation/audio/main work. Debug properties cleared after testing. No data-memory writes without observer integration; no automatic atomic step-over, watchpoints, x87/YMM, TF-observing instruction Step, mixed unwind, provider UUID or white-screen/root-cause acceptance. No full regression. See artifact manifest for final host/JNI/APK identities.

- **Guest debugger / post-PLAY zero-FPS audit (2026-09-15): [current capabilities and evidence](docs/validation/android-native-host/guest-debugger-zero-fps-audit-2026-09-15.md).** Read-only audit, no production repair. Safe-point snapshots, owner/epoch control and HleScope callbacks exist; Step remains Unsupported, production guest RSP/breakpoint lifecycle/mixed unwind and arbitrary-JIT full-state reconstruction do not. Do not read CPUState.rip or GetGuestBlockEntry as an exact asynchronous guest PC; t_binding is intentionally null during native HLE. User identifies Circle/PLAY transition, not cold startup. PID22870 changed Session generations during capture; never combine generation1 menu58FPS with generation2 initial zero counters. Pre-debugger generation2 ring shows active high-frequency mutex/cond and audio consumption/xrun0; generation3 host samples include VM-gate contention and semaphore waits, but do not prove the original white-screen cause. LLDB cleaned/target alive/TracerPid0. Debugger pauses perturb timeouts, including Oboe's two-second callback watchdog. Next useful diagnostic capability is bounded owner/wait-object/producer attribution plus automatic stall evidence, not a rewrite of the Linux gdbstub. No white-screen/playability acceptance; preserve existing dirty work.

- **Latest Oboe audio implementation (2026-09-15, uncommitted): [delivery/evidence](docs/validation/android-native-host/oboe-audio-integration-2026-09-15.md).** Production GuestAudio now pins briefly and converts directly into preallocated host stereo blocks; Foundation audio queues borrow descriptors, with batch publication epochs and variable callback block stitching. No extra PCM ring, guest pin across waits/callbacks, or per-port worker/software pacing on this path. Session ports share an Oboe endpoint; desktop downmix/gain, bounded credits, watermark drain, callback retirement, controlled/automatic reconnect and diagnostic counters are integrated. Oboe remains independent at owned `987538b6` / `codex/shadps4-audio`; Foundation audio is local, not a published pin. Final native123/0; Foundation40/0 incl ASan/UBSan/TSan and callback allocations0; ordinary APK real-FEX13 imports×32 buffers×3 same-PID rounds. TMNT120s:3909 presents, Stop CANCELLED, sustained audio consumption, sampled xrun0/error0; no input progression/pixels/audibility/playability acceptance. Actual APK callback mode still gets perf NONE (burst960/buffer1920), not FAST/MMAP. Host badb52cd / JNI3c0867b6 / APK e6f92f07. FEX implementation/mutex remain unchanged this turn; next independent line is compiled guest fast path plus host/FEX bridge slow path. Preserve previous uncommitted changes. No full regression.

- **Latest FMOD / Android audio design audit (2026-09-15): [source comparison, probes and implementation direction](docs/validation/android-native-host/fmod-android-audio-design-audit-2026-09-15.md).** FMOD Core/Studio is proprietary; game Core version is statically verified 2.02.26. Public flushCommands/flushSampleLoading share the observed wait helper, but sampled parents cannot distinguish the active top-level API or prove the pending object's writer. Earlier loading PROF still has ~48k frames/s AudioOut progress; do not call it a dead audio device. Production blocking AAudio logs show LOW_LATENCY downgraded to NONE. Same-UID standalone AAudio/Oboe silent probes also get NONE; these are runas_app, not ordinary APK acceptance. Citron local6a86baf4 forces Oboe/OpenSL; Azahar254bff35 uses cubeb with AAudio preference and native DSP timing. Prefer Oboe for the host device endpoint, with owned bounded PCM, explicit downmix, consumption watermarks and generation lifecycle; keep Orbis validation/backpressure/completion in shadPS4 and callback free of guest/FEX/VM calls. Foundation audio is a proposal only; preserve independent Oboe/cubeb subrepos. No production code/device-game run/child changes this audit, no low-latency or loading/white-screen/playability fix claimed. Preserve previous uncommitted work and targeted-check policy.

- **Latest local startup/audio review (2026-09-14, uncommitted): [profile and desktop comparison](docs/validation/android-native-host/startup-audio-profile-2026-09-14.md), [ring/clock/compiled guest evidence](docs/validation/android-native-host/ring-clock-compiled-guest-2026-09-14.md).** Real 190 s PROF + simpleperf identifies GuestAddressSpace and HLE/mutex contention during ~78 s startup and a ~55 s recoverable FMOD loading wait. Audio device waits run on dedicated audio owners, not the guest main thread. Android AudioOut now has two owned pending PCM blocks plus one worker-consumed block, bounded backpressure and cancellable drain/Close; native 68/0. Desktop comparison found AvPlayer try-lock contention falsely returning EOF; normal owners now wait cancellably, callback reentry remains nonblocking, deterministic negatives change 4 FAIL to PASS, expanded cancellation test1204/0. Address sorting/binary lookup was withdrawn after pure-color rendering appeared; 16 s first flip/60 FPS is NOT accepted performance. With linear lookup, portal renders but post-Circle white transition still reproduces, so rollback does not prove a root cause. Final APK b65fe36a/host ce4fea22, PID21732 has205s portal image and actual guest Circle press/release, but no next scene; actual APK Stop reaches Stopped/user_stop and closes audio streams; this round does not reproduce white. Do not claim white screen/loading/playability fixed. Current code preserves the audio queue and AvPlayer repair; investigate the real transition. FEX child unchanged; main adapter disables SMALLTSCSCALE after Reload (G50 3/0); compiled C/C++ guest mutex is prototype13/0, production mutex stays host-owned. Foundation8a564bf/3f30145 and profiler_ring module are local, not verified published pins. No auto tag, no full regression.

- **Latest font/R8 repair and real capture (2026-09-14): [root cause/evidence](docs/validation/android-native-host/font-r8-tiling-fix-2026-09-14.md).** Actual TMNT Font/FontFt imports absent; guest atlas CPU decode is intact. Selected Turnip has shaderInt8=1 but storageBuffer8BitAccess=0. Host R8 tiler/detiler now uses packed32 storage; independent real GPU6/6 exact1MiB comparisons, ordinary APK text clear, StatusLayer/full1920×1080 retained. Host8730d4865daeb17bf2b62f2e27e002377a39f3b5; APK44af23b0… exact manifest in font-r8/artifacts.json. Real223818013byte RDC9ee84736… and same-Turnip Android replay now verified; cmake/renderdoc contains paired6ae929af replay-loader patch, no system fallback. Capture settings/property/controller cleaned; SDK provider for GPU Reshape replay not bundled. Prior no-RDC/corrupt-font statements below are historical. Actual guest8bitSSBO lowering/capability declarations remain a separate gap, as do Font-provider compatibility for other games, fullPROF/Layer/PM4 and playability; do not claim these from the R8 repair. FEX/Foundation unchanged.

- **StatusLayer / immersive follow-up (2026-09-14):** Native Presenter-owned FPS and 240-frame timing graph are visible on the real primary display; reused frames excluded, stalls preserved, no GPU-time claim. Shared SessionSystemBars fixes debug Surface host and production focus/Insets recovery; real APK120s Stop passes, full1920×1080, status/navigation bars invisible after notification panel dismissal. Actual guest dialog text is still corrupted. This is StatusLayer content in the existing ImGui context, not full Foundation Layer/litep migration. See graphics-toolkit delivery and immutable stage artifacts.

- **Latest direct graphics-debug/concurrency repair (2026-09-14): [delivery and limits](docs/validation/android-native-host/graphics-toolkit-repair-2026-09-14.md).** Previous capture/identity/counter review findings repaired directly, no new spec. Optional Cemu-style GPU Reshape SDK integrated on private Turnip, descriptor increment + indirect hooks + bounded collector/ambiguous SGUID corrections. Independent SDK remains a dirty external checkout: main stores additive patches and exact source/binary manifests, not a newly published SDK pin. Citron bounded pipeline_handoff recorder + physical-thread interval analysis/export are implemented (not full PROF/litep/Layer). Fixed Liverpool queue/count publication, queue lock scope, frame-ready wait stage, recreate synchronization and cancellable priority timeline worker. Portable242/0 + Python12; SDK forwarding38/0. Ordinary APK120s OFF and single-shader runs Stop cleanly; actual physical display0 shows logo/background/dialog with **corrupt text still present**. Old scrcpy black-only captures are not reliable pixel evidence. Single shader26af93b82752ceb9 really replaced1 module, collector1147 complete, no findings/overflow; not whole-GPU bounds acceptance. All-shader mapping alerts/implicit descriptor safeguard remain unresolved. At that earlier stage no real RDC/replay; latest capture/R8 entry supersedes this. Complete PM4 trace/PROF/Layer and playable/10min/Swan acceptance remain open. Keep Turnip, targeted tests, auto tag deferred, FEX/Foundation unchanged. Latest artifacts/tests and source identities are in the delivery; do not repeat old coordinator/counter deficiencies as unimplemented.


- **Toolkit review at `30e3b21f` (2026-09-14): [findings and continuation](docs/validation/android-native-host/graphics-toolkit-progress-review-2026-09-14.md).** §3.1 is partial and §3.2 is not accepted: default capture starts/ends in one present callback; timeout leaves backend active; status waits on EndCapture; cancel/session/UUID ownership is incomplete; Android never calls LoadRenderDoc. QueueSubmit measures guest enqueue, HostPresent is only mirrored at later guest submits/Run return, phase/Stop reasons are unwired. Existing native173/0 reproduced, alongside12 injected-backend counterexamples; prior APK2/0 only proves control/partial producers/absent-RenderDoc path, not actual capture/replay. Fix this continuous control/capture package before treating it complete; then continue PROF/Layer/PM4 under the original spec. Foundation migration is already in scope; ad-hoc ImGui include/config errors do not prove a required full UI migration. No production repair was made by this review. Auto tag remains deferred.

- **Next implementation: [Android graphics debugging toolkit spec](docs/specs/android-graphics-debugging-toolkit.md), [source audit](docs/validation/android-native-host/graphics-debug-tooling-audit-2026-09-14.md).** User requests RenderDoc, litep/PROF, Foundation ImGui Layer, decoded PS4 guest command trace and raw PM4/GCN evidence as one integrated delivery for Opus 4.8. **Auto tag is deferred by the latest user decision; no FEX probes/guest code patching in this stage.** This is a plan only. Two continuous work packages: control/profile/Layer/capture, then submit provenance/PM4+action trace/decoder/correlation. Preserve Turnip, RelWithDebInfo, ordinary APK and targeted tests. Current RenderDoc NOLOAD discovery and capture boundaries need repair; Android Tracy remains intentionally OFF; current ImGui and Foundation versions differ. Reference Foundation profiler branch2b2683ff is published; its SDK remains an independent code.byted.org submodule. Citron local6a86baf4 is NOT available on the remote branch (still2106bcd8); do not invent a fetchable pin or push unrelated Citron work. See spec for exact ownership, commands, bounded cancellation and acceptance.

- **Latest post-button NP fault repair (2026-09-14): [evidence](docs/validation/android-native-host/np-offline-identity-fix-2026-09-14.md).** `sceNpGetAccountIdA`/op56 Unsupported is fixed. Shared desktop offline identity policy plus session SDK/signup snapshot covers 11 queries + 2 existing polls, bounded guest outputs and strict library/online gates. Native339/0; x86 NDK syntax2/2; ordinary APK real-FEX13-import three rounds PASS. TMNT actually calls account/online ID and gets SIGNED_OUT, advances into account-specific saves; single120s/Stop CANCELLED/JUnit PASS but **only161 presents and sampled black screen**, so rendering/playability remain open. No network/SSL expansion, no full regression. Host/JNI RelWithDebInfo, FEXCore Release, playstoreDebug APK; artifact IDs and dirty-source hashes recorded. FEX/Foundation unchanged. This supersedes the earlier op56 fault and does not replace earlier graphics evidence with a playable claim.

- **Latest local AvPlayer/save-directory/Stop delivery (2026-09-14): [implementation/evidence](docs/validation/android-native-host/avplayer-directory-integration-2026-09-14.md).** 21 AvPlayer NIDs reuse desktop local decode with session handles, checked guest buffers and persistent FEX callback owners; no host pointers escape, no VM pin across callbacks/waits. Save-root O_DIRECTORY EINVAL caused TMNT's post-video invalid index; descriptor-relative enumeration now reuses desktop NormalDirectory, four directory NIDs, stat/preadv/seek and Logger stdio. Native AvPlayer1042/0 (earlier DSO), final directory334/0, graphics admission9/0; x86 NDK syntax4+3 (not desktop runtime). Latest ordinary APK: synthetic AvPlayer3 same-PID rounds PASS; **real TMNT single120s run,2525 presents, Stop CANCELLED/JUnit PASS**. Game dialog appears but text is corrupted: no correct-pixel/playable/ten-minute/three-real-game-restart/Swan acceptance. GNM Busy-on-cancel and AvPlayer callback VM drain after GPU Stop were reproduced and repaired; global Stop retains callback mappings until whole-session GPU/worker teardown, normal Close still releases them. Preserve prior failures/artifact identities; FEX/Foundation unchanged. User: network/SSL need no new implementation, desktop offline/dummy only. Continue actual graphics/input/game correctness directly, targeted checks only, no new spec. Supersedes old AvPlayer Unsupported/zero-interface claims below.

- **Latest SRT/BC/WSI delivery (2026-09-14): [implementation/evidence](docs/validation/android-native-host/srt-bc-integration-2026-09-14.md).** Modern desktop IR analysis now produces a checked portable SRT plan on ARM64; duplicate-load aliases, cache architecture/version, BC macro/micro mip selection and unchanged-capability Android SUBOPTIMAL handling repaired. Native479/0 (including cache-prefix checks), x86 syntax4/4, shader8 variants, ordinary APK synthetic6 generations PASS. Latest complete-content TMNT143 guest presents then **sceAvPlayerSetLogCallback / eBTreZ84JFY / op110 / Unsupported**, so120s JUnit remainsFAIL and no usable game image/playability/Swan acceptance. Current WSI excerpt has1 unchanged-capability message and0 recreates. FEX/Foundation unchanged. Supersedes prior ARM64 SRT unimplemented/zero-frame boundary. Continue desktop local AvPlayer as owned handles/buffers/FEX callbacks/cancel; not implemented yet. Network/SSL remain desktop offline compatibility only, no new spec or full regression.

- **Latest AudioOut/semaphore delivery (2026-09-14): [implementation/evidence](docs/validation/android-native-host/audio-semaphore-integration-2026-09-14.md).** Session-owned AudioOut9 NIDs and kernel semaphore6 NIDs now reach actual FEX/AAudio. Short-write/tail recovery/gain/open-failure/cancel repaired; no VM lock/pin across waits. Native55/0 +55/0; Json2/sysmodule35/0. Precisely match desktop optional Json2 load bookkeeping in a separate compatibility table, not a real provider or new JSON/network/SSL implementation; unknown missing modules/functions still fail. This eliminates the real guest invalid-free after negative Json2 load. Ordinary APK synthetic13-import3 rounds; TMNT3 same-PID20s runs cancel cleanly with graphics=ready but **guest_presents=0, not playable**. Old crash/45s expectation failure preserved. Longer undisturbed APK observation actually aborts at ~51s in first compute shader: ARM64 FlattenExtendedUserdataPass is unimplemented. Fix modern SRT semantics directly; old reference handles only static offsets. No proven VM deadlock. FEX/Foundation unchanged; source8762e541+dirty and stage-specific artifact identities retained. Continue direct zero-frame/cold-start diagnosis, desktop-first, targeted checks only, no network/SSL expansion or new spec. This supersedes the AudioOutInit boundary below.

- **Latest AJM delivery (2026-09-14): [implementation/evidence](docs/validation/android-native-host/ajm-runtime-integration-2026-09-14.md).** Session-owned async GuestAjm reuses desktop decoders with checked guest batch/PCM/sideband copies; all output mapping identities (including interior splits/remaps) are rechecked under the short VM gate before publication. No VM gate/pin across decode/Wait; stop-token frame boundaries and worker join before VM teardown. 15 explicitly bound NIDs; no guessed Finalize/ModuleUnregister stub ABI, FIFO/desktop statistics estimates and AAC S32 rejection documented. Shared MP3/AT9/AAC input/initialization error boundaries hardened. AYN/API33/4KiB native126/0 with real synthetic MP3 decode32256bytes/8064samples; ordinary APK8/10-arg FEX batch3 rounds; full-content TMNT3 same-PID rounds now at **sceAudioOutInit / JfEPXVxhFqA / op95**, graphics=ready, guest_presents=0. No game audio/frame/Swan/full-regression acceptance. Source8276deb7+dirty, exact deployed hashes in latest-build; FEX/Foundation unchanged. Continue AudioOut family and AAudio short-write/gain/open-failure repair directly; no new network/SSL work or micro-spec.

- **Latest module/libc repair (2026-09-14): [zero-offset DT_INIT and thread destructors](docs/validation/android-native-host/module-init-thread-dtors-2026-09-14.md).** Shared Module now distinguishes a missing DT_INIT from its legal zero offset. Actual libc/FMOD/FMOD Studio constructors had been skipped; old “six DT_INIT completed” logs overstated execution and are superseded. Exact host LLDB/JIT metadata located the resulting FMOD null member; no FEX change. LoadStartModule reuses already-loaded guest modules without rerunning init or changing pRes; Dlsym only returns checked guest exports. New dynamic ELF/TLS load/unload remains explicitly Unsupported. Runtime-owned libc thread hook executes through FEX with owner TLS before key destructors, for both return and pthread_exit; validated full-width Count/Report registrations and ASan-off null queries complete the constructor surface. AYN/API33/4KiB ordinary APK: DT_INIT zero/nonzero/absent matrix9 rounds, 12-import destructor4 rounds, full-content TMNT3 same-process rounds now reach **sceAjmInitialize / dl+4eHSzUu4 / op385**, graphics=ready, guest_presents=0. No game/Swan/full-regression acceptance. Preserve source61805270+dirty artifact identities and debugger cleanup evidence. Continue AJM with checked batch buffers and session-owned decoders, following desktop implementation; no network/SSL expansion or new micro-spec. FEX/Foundation unchanged.

- **Latest direct delivery (2026-09-14): [offline compatibility, condition/VM and production Pad](docs/validation/android-native-host/network-condition-pad-integration-2026-09-14.md).** Native AYN/API33/4KiB: network199/0, condition66/0, Pad85/0 (stage-specific DSOs); APK52-import callback cancellation/recovery5 rounds, clock-vs-VM3 rounds×128 map/unmaps, production Pad10-import3 rounds via actual app InputHub. Final full-content TMNT3 same-PID rounds now stop at **sceKernelLoadStartModule wzvqT4UqKX8 / op44 / Unsupported**, zero presents. Pad16 NIDs use existing host OrbisPadAdapter/Foundation; captured input token, checked outputs before history consumption, real haptic queue and stale handle/token tests. No IMU/lightbar/physical-game-control claim. General short Write serializes with VM publication; never hold the gate across waits/callbacks. Cond relative/absolute timeout/attrs/targeted signal retain mutex reacquisition and Stop cancellation. Network callback uses actual InvokeGuest, self-unregister and entered-callback cancel/recovery. **User update: network and SSL need no new implementation at this stage; follow desktop compatibility.** Preserve existing offline control/lifecycle; no DNS/socket/HTTP/TLS expansion. SSL Init/Term directly reuse desktop Ssl2 dummy compatibility; no TLS/provider capability claim. Final host Build ID e3059cf93568536e786548a280b78845808e2c1c, JNI253444d30ea4b0b7e14571b620fc35808b03e650; source/artifacts in latest-build.json. FEX/Foundation unchanged. Continue actual LoadStartModule path/init/TLS work, targeted checks, no new spec/full regression. This supersedes older NetInit/PadInit boundaries below.

- **Latest sysmodule/file/RTC/AppContent increment (2026-09-13): [delivery and exact limits](docs/validation/android-native-host/sysmodule-rtc-content-integration-2026-09-13.md).** WithArg to already-initialized providers, five DiscMap entries, stat/fstat/positioned scalar+vector I/O/truncate, 42 RTC entries and eight AppContent entries are connected. Shared desktop GetTick preserves input and avoids overflow; RTC text helper is now shared by desktop and guest adapters, with checked grammar/timezone and atomic output. Precise aliases retain desktop two-digit output, not a new precision guarantee. AYN/API33/4KiB: sysmodule30/0; final RTC397/0; file/DLC/temp144/0 on the preceding DSO; latest22-import guest3 same-PID rounds. Complete base+update PFS content (44 files/1,868,512,653 bytes, per-file SHA checked) now passes AppContent/init/temp/RTC text and reaches **sceNetInit Nlev7Lg8k3A / op312** three same-PID rounds, zero presents. AppContent owns metadata/DLC inventory, actual read-only directory mounts and entitlement events; /temp0 owns a fresh private directory per session, real file I/O/space and cleanup after closing fds. UnmountOwned checks both mount name and host root. Temporary legacy Mount/Format/Unmount NIDs remain closed because desktop prototypes are stubs; internal lifecycle is implemented/tested. The earlier disconnect and artifact mismatch are historical, superseded by apk-fourth through apk-sixth and latest-build.json. New ELF dynamic loading/initializer arguments/dynamic TLS-unload, clock mutation and DLC download/archive mounts remain open. No fabricated keys/network clocks or fake NetInit success. FEX/Foundation unchanged. Continue network net/netctl as a session-owned family, directly and with targeted checks, no full regression/new micro-spec. Historical boundaries below are superseded by this entry.

- **Latest libc/thread delivery (2026-09-13): [implementation/evidence](docs/validation/android-native-host/thread-libc-integration-2026-09-13.md).** This supersedes the old pthread_attr_init boundary. Production now prefers configured guest `libSceLibcInternal.sprx`, with normal dependency/TLS/_malloc_init/DT_INIT and no compatibility mixing after provider selection/failure. Without it, an explicit 112-NID policy reuses unique real guest libc exports (102 present in the audited game; current 27 Internal imports resolved, not all-function execution coverage). Native ARM64 HLE pointers/FILE/va_list are never substituted. No real system firmware libc was tested; synthetic provider selection/init-failure/removal recovery passes five APK modes. Session thread attrs are actually consumed by pthread_create (stack/guard/detach/affinity/scheduling metadata); rwlocks and mutex protocol/ceiling/legacy-kind semantics are integrated. Desktop mutex protocol also lacks real-time priority donation; preserve its existing emulation semantics, not an Android scheduling guarantee. FEX syscall-to-HLE now notifies the waiting VM coordinator and retires armed poll-page protection without consuming tickets. Mutex/rwlock short writes and attr operations serialize with the production VM mutex, never retaining a pin/VM gate across guest lock/cond waits; preserve address-space Busy exclusion. First background fault is propagated with original owner attribution. Final targeted AYN/API33/4KiB: attr43/0, locks60/0, FEX G49/G48 focused6/0, three APK JUnit selectors. **TMNT remains unrendered: three same-PID rounds now reach sceSysmoduleLoadModuleInternalWithArg (hHrGoGoNf+s, op527, Unsupported), guest_presents=0**. Selected executable/modules only, no full regression/Swan/game acceptance. Final host Build ID ece7a428f2168c7c63f68c29f00f6a951f5683b0; JNI5adbce8c4a94be85905f68e347d6650aa7232089; preserve f4ba0623+dirty source manifests. FEX/Foundation unchanged.
- **Execution rule (user, 2026-09-13): inspect desktop before filling each gap.** Read registration, actual implementation, provider/init/callback dependencies and existing desktop limitations first; migrate complete function families and their runtime ownership together. Do not repeatedly hand off one new NID as a micro-spec. Continue direct implementation and targeted checks of changed behavior; do not run a full regression unless new evidence or user instruction warrants it. Full system libc preference and fallback policy must be considered together, not just expanding an allow-list. Dynamic sysmodule loading is the next observed subsystem boundary; preserve real provider readiness/refcounts/init arguments and never return fake success for absent modules.

- **Latest graphics/storage delivery (2026-09-13): [implementation and evidence](docs/validation/android-native-host/graphics-storage-integration-2026-09-13.md).** Native GNM/VideoOut checked policies, owned command queues, guest labels/embedded shaders, GPU watch/FEX fault delivery, VM/GPU admission/drain, IRQ/equeue lifetimes and actual present counting are now connected. Ordinary AYN/API33/4KiB APK: six same-PID generations, three real FEX synthetic GPU rounds each present four frames; no system-driver fallback. Storage/SaveDataDialog are implemented, with guest create/backup/restore/read and changed APP_VER across different APK PIDs; actual Compose Cancel reaches guest. Session semaphores also implemented. Host511/0, FEX244/0, CPU46/46, ABI14/14, services70/0, Session840/0. **TMNT remains blocked at pthread_attr_init (wtkt-teR1so, op280), guest_presents=0**, selected executable/modules only; neither game nor Swan acceptance. Attribute init must be composed with actual pthread_create attribute handling (currently non-default returns ENOTSUP), not a fake successful initializer. Preserve explicit unsupported policies and the documented GPU driver/PM4/dialog limits. FEX and Foundation unchanged. No new spec; continue actual runtime/game blockers. This bullet supersedes earlier claims that GPU submission/fault delivery and all save support remain unimplemented.

- **Save support is required (user clarification, 2026-09-13).** `libSceSaveDataDialog` (the missing sysmodule id0xa0) is the save interaction layer; `libSceSaveData` implements persistence. Both are now integrated as recorded above; retain the acceptance requirements: reuse the existing save backend with session-owned state, checked guest structures/nested pointers and guest file-system mounts; persistent saves must be isolated by stable user/title identity, survive Session/process restart and base+update replacement, and remain outside temporary content/generation directories. Android dialog status/results must reflect actual user actions or actual operation completion, with cancellation/Stop cleanup; never auto-report success. Acceptance includes real guest create/write/unmount/readback after restart, update retaining saves, and meaningful bad-pointer/read-only/I/O-failure/cancellation checks. Desktop globals and a fake successful module load are insufficient. No separate micro-spec is needed.
- **Latest direct WP2 integration and WP1 corrections (2026-09-13): [review/delivery](docs/validation/android-native-host/wp2-native-turnip-runtime-review-2026-09-13.md).** Starting from `10a5b23e`, ordinary APK now uses checksum-pinned bionic Turnip (`runtime/locks/turnip-bionic.json`, R8 archive; actual driverInfo Mesa26.0.0-devel/5ac41be677, shaderInt64=1), four packaged adrenotools hooks, a process-lived loader and generation-owned AndroidWindow/Presenter/VideoOut/IRQ. Hooks must NOT be JNI DT_NEEDED; no system fallback. Service connects input before PlatformReady; Surface loss stops its generation. Actual guest VideoOutOpen/Resolution/SetBufferAttribute/Vblank run through local-copy/pin wrappers; seventh SysV argument uses CallCursor. Real selected TMNT now reaches **sceVideoOutRegisterBuffers (w3BY+tAEiQY, op153)** after actual renderer creation; no game frame/full assets/Swan acceptance. GPU registration/submit/flip, label lifetime and tracking/fault-delivery/VM integration are still refused, not secretly enabled.
- **Closed ownership bugs in this review:** dependency names are not provider readiness; GuestSysmodules publishes only initialized providers with stable handles/refcounts. GuestPlatform owns user/system state per session; no desktop event queues. Bind blocks libkernel allow-set bypass. Passive session SignalDispatch prevents PageManager from replacing FEX/ART handlers; it does NOT yet implement GPU fault recovery. Android assertions preserve debuggerd stacks without resetting those handlers. The main FEX adapter now atomically reacquires continuation admission with running publication, retries a competing epoch within one deadline, and preserves external Cancel. Production internal Pause does not poison the HLE cancellation source. Thread creation/destruction serializes with the production VM mutex. G48 forces 33 release/admission collisions plus Cancel while a second token is held; existing FEX child remains unchanged. Final AYN/API33/4KiB: FEX244/0 (G48 included), CPU46/46, ABI14/14, registry13/0, veneer19/0, services70/0, Session840/0, host198/0; CLI10×3, ordinary Service APK16 same-PID generations, Turnip2 JUnit (3 WSI generations), synthetic graphics5 generations and selected TMNT3 generations. Host Build ID9c8df3d6af500a24994f3c3f388ca1b37843eb07; JNI b903766b1155f68af99c7c6369156072faeafda4. See delivery manifests for identities. The prior [WP1 report](docs/validation/android-native-host/wp1-hle-families-2026-09-13.md) is historical: its claim that all non-graphics startup was complete was too broad, and its VideoOutOpen boundary is superseded. Do not restart initial runtime/window/driver integration or create another spec for this completed batch.

- **Latest direct runtime repair (2026-09-13): [libc/startup review and fixes](docs/validation/android-native-host/libc-runtime-repair-2026-09-13.md).** The `d499219e` report's null-deref boundary is superseded: production now calls guest `_malloc_init`/optional libc mutex enable before DT_INIT, corrects `0-KXaS70xy4` to pthread_getspecific, uses session clock/guest errno, and implements bounded mutex/cond/TSD plus allocator-startup VM/heap/proc interfaces. Known Internal stream/tag imports use actual guest libc exports under a narrow policy; no fabricated 8-byte marker or blanket libc/Internal alias. TSD destructors use existing guest callbacks; condition lifetime includes mutex reacquisition, with Busy-on-destroy regression. Final AYN/API33/4KiB: services43/0, FEX guest242/0, CLI10 groups×3, ordinary APK16 same-PID generations including allocator-init cancellation/recovery. Exact source/APK host Build ID446ec4a0d98923fd62bd03d2796c74a484885cb5; evidence retainsd499219e+dirty source identity. Real TMNT selected-content shell run completes six dependency DT_INITs then structured-faults on sceSysmoduleLoadModule (now superseded by the WP1 family expansion above); not game/Swan/rendering acceptance. Do not infer exact guest RIP from GetGuestBlockEntry or put Android logging in signal handlers; general SIGILL/SEGV recovery remains open. Continue the existing integrated TMNT spec from session-owned sysmodule/provider/platform readiness, not another initial runtime/libc bootstrap rewrite.
- **Current production-runtime baseline (2026-09-13): [review/repairs](docs/validation/android-native-host/wp1-mechanism-review-2026-09-13.md) → [next integrated TMNT spec](docs/specs/android-native-host-tmnt-after-runtime.md), [canonical evidence](docs/validation/android-native-host/2026-09-13-wp1-review/README.md).** Production Module/Linker, segmented guest VM/backing, DT_NEEDED/DT_INIT, independent guest pthread/TLS/errno, guest stack guard, HleScope/two-level InvokeGuest/cancellable WaitingHle are now composed in GuestRuntime and bound through FexSessionBackend/JNI/the ordinary Service. Do not hand their initial integration to the next AI again. AYN/API33/4KiB: guest242/0, contract46/46, ABI14/0, registry13/0, veneer19/0; ordinary APK12 same-PID generations (ELF/SELF, two modules/TLS/init, child execution plus VM, return/cancel/bad-pointer/unknown-import/init-cancel and recovery). Host dlopen+85/0; modern macOS contract45+1SKIP/46, Session807/0. These are synthetic runtime acceptance, not game/Swan/full R2 acceptance; evidence retains07ce52ae+dirty source identity. Next AI expands actual Orbis/game HLE and Turnip/platform/content, then TMNT10min/three same-process game restarts.
- **Runtime invariants:** host DSO alone owns GuestAddressSpace/InputHub; JNI/FEX consumes its generated SDK. FEX remains385a0cc4; keep rpmalloc, ordinary mmap/munmap allocator hooks, no high-VA steal/global bionic replacement. Production owns4MiB–256MiB and4GiB–120GiB only, leaving ART hole untouched; collision fails/reclaims. G46 verifies full tags across64GiB; M32 verifies hole exclusion. The main-repo private EntryBackedgePass (after RA, no new allocated registers) routes guest-entry branches through ExitFunction so warmed loops still poll; instruction-internal loops stay intact. G47 warms≥10000 guest iterations before cancel. Do not remove this pass, reintroduce SleepThread parking, set MAXINST=1, or call old SetupHooks. HLE dispatch occurs after NON-spill JIT exit, scoped callbacks preserve outer state and fault/cancel/exit attribution; internal VM pause cannot clear user Cancel.
- **Loader/content boundary:** production loader handles SELF logical section offsets without treating them as physical container offsets; ordinary ELF tables remain checked. Resolve dependencies under sce_module/, modules/, then content root with canonical containment. __stack_chk_guard is explicitly guest-owned readonly data; unknown objects still reject and unknown functions use named failing operations. Never put native HLE/global pointers in guest relocations. Latest real TMNT base+update executable/module selection and earliest boundary are recorded in this review; complete assets/UI/version lease and game compatibility remain subsequent work. General guest SIGILL/SEGV recovery, dynamic module/TLS unload and unimplemented Orbis API families are not silently accepted by synthetic tests.

The dated milestones below retain historical context. For implementation state and pending work, the current baseline and next TMNT spec above supersede earlier “no runtime/no HleScope/CPU smoke only” descriptions.

- **Earlier input baseline (2026-09-13): [runtime/input review and repairs](docs/validation/android-native-host/runtime-input-review-2026-09-13.md) → [next integrated execution spec](docs/specs/android-native-host-runtime-after-input.md).** Repair delivery98eb0534 (validated as768636aa+dirty patch); Foundation owned branch now5388ef45313d6c32cb5f4bb5b07f1246ee381370, child already pushed. The ordinary APK really loads host plus FEX/JNI DSOs; host alone owns the production OrbisPadAdapter/InputHub. Foundation Android source, app bridge and actual scePad read/vibration exports are connected. Preserve session/device epochs, ordered packets/feedback, source merge/history, main-looper actuator pump and generation-scoped Stop (never change Activity focus to stop a session). Final AYN/API33/4KiB: host dlopen+85/0, Foundation portable49/0 and Android5 tests, Orbis adapter45/0, APK input6 tests, actual Service1 test/3 same-PID rounds with real pad press/release and CPU Running/Cancelled. Hardware key events were injected; physical vibration, custom remapping, FEX-origin pad and game rendering are not accepted. Host has no SDL/JNI_OnLoad or initial-exec TLS: Android optional Tracy is disabled because its pinned embedded rpmalloc fails ART dlopen; FEX allocator is unchanged. No microphone returns NOT_OPENED, never fake silence. Current two-DSO graph is valid; do not duplicate host globals or remerge just to claim one library.
- **Stage0 correction:** old768636aa report does not prove TMNT crt/first HLE execution. Its harness omitted PT_SCE_RELRO and filled real data with zeros, ignored unresolved relocations/TLS and misread native pc0 as guest RIP0. The repaired optional harness is load-audit-only (no FEX archive): 11 cases PASS; real image3 segments/1RELRO/92929RELATIVE/406symbol/652PLT/TLS40144; EXECUTION_NOT_RUN, --require-execution exit3. Preserve old evidence with correction. The production runtime is now implemented and verified above; the old harness stays load-audit-only. Continue Orbis game coverage and Turnip/Surface/audio/base+update/game10min/three game restarts under the latest spec.

- **Native host library milestone (2026-09-12, source `1149e948`): [delivery/evidence](docs/validation/android-native-host/host-library-milestone-2026-09-12.md) → [updated production Runtime spec](docs/specs/android-native-host-production-runtime.md).** `libshadps4_host.so` now really links with `--no-undefined`, arm64/API33/c++_shared/Foundation ON; 388 host TUs. Actual AYN/API33/4KiB/shell loading and contracts pass61/0; macOS portable42/0, desktop-adapter NDK syntax3/3. Reuse `scripts/android/build-host-android`, `cmake/host-tools`, `shadps4_host_objects` and `shadps4_host_dependencies`; no manual host archive list or `/tmp` tool prerequisite. Source hashes match1149e948; binary SCM retains original build-time7157f801 dirty identity. Window/AndroidWindow/shared Presenter ownership, identity-checked publication, ApplicationControl and launcher/settings separation are implemented but not bound to production SessionCore. ARM64 Ucontext initialization/fail-closed and epoll ceil conversion are fixed; actual FEX guest snapshot/handler/writeback is still open. Android `path_util` DSO constructor crash was reproduced and fixed: explicitly call `InitializeAndroidUserPaths(filesDir child)` before host services, catch failures, do not set HOME or reuse desktop implicit paths. Init retries after failure, then layout is immutable. The old nine-symbol failure review is historical. At that milestone the APK was CPU smoke only; current APK host/input loading is verified above, while production game/Turnip integration remains open. Preserve old evidence.
- **Input architecture origin (2026-09-12):** [Foundation input-first design](docs/specs/android-foundation-input-first.md) and [original review](docs/validation/android-native-host/foundation-input-review-2026-09-12.md) retain the low-coupling contract. Common device/event/state/haptics live in Foundation `modules/input`; application JNI/session and Orbis semantics stay here. Implementation and evidence now exist as described above; do not repeat the old “plan only / no consumer / host still SDL” state. Citron reference stays2106bcd83844e05dfbb5251a902143233a7ec43b. Full custom profiles/settings, physical actuator and game-origin acceptance remain open.

- **Bionic migration handoff (2026-09-12): [two-work-package plan](docs/specs/android-native-host-full-link-plan-2026-09-12.md), [prerequisite results](docs/validation/android-native-host/bionic-prerequisites-2026-09-12.md), [third-party ownership/reuse audit](docs/validation/android-native-host/bionic-third-party-audit-2026-09-12.md).** Five shared recipes (date/glslang/Zydis/miniz/LibreSSL) have real NDK probe linkage, AYN CLI8/8, host8/8, wrapper failure4/4. Android FFmpeg now uses the independent owned `externals/ffmpeg` at `e17ba6e2` (upstream n7.1.5), branch `codex/shadps4-bionic-7.1`: six matching libraries, media syntax7/7, AYN media9/9 including actual H264/COPY_OPAQUE/scale/resample. Do not reuse citron's older codec59 package or put FFmpeg into Foundation. `externals/libadrenotools` pins azahar's `60ae5bbc` with linkernsbypass `aa397589`; five NDK DSOs link, but real Turnip/ordinary APK loading is NOT integrated. Preserve all four hook libraries and package/extract them explicitly. Root externals now keeps spdlog on the pinned local fmt and requires Android OpenAL OpenSL; existing healthy third-party pins/Foundation/FEX remain unchanged. The expanded production-externals probe now passes AYN14/14: hwinfo's x86 glibc-loader-based bitness error was repaired on new owned `tencentmalos/ext-hwinfo` branch `codex/shadps4-bionic`, pin `85bbcba3`; Android reports the native process ABI. gh reauthentication and owned refs are verified; retain the historical13/14 failure evidence, but do not carry this fixed issue forward. Continue complete host/platform/Turnip/APK, then real PKG, without per-library handoff. Dependency probes are not full host or APK acceptance.
- **Current execution: [COMMON/Git progress review](docs/validation/android-native-host/pkg-v2-common-review-2026-09-12.md) → [PKG v2 integrated execution spec](docs/specs/android-native-host-pkg-v2.md).** Graphics/WSI/script repairs are committed as `091334d3` and entry-point sync as `1d411955`. Code base `7c675280` includes COMMON/bionic adaptations `06bd43bd`. Main history plus omitted specs/research/evidence were pushed through `2efe7004` (archive commits `bb4d6c4a`/`2efe7004`); this latest review is delivered afterward. Fresh COMMON4/4 objects and crypto/ipc include checks pass. Both review findings are now fixed in the main repo: actual-return-type strerror_r handling and 16-byte ESR-record layout validation. Formal tests/common targets pass on AYN/API33/4KiB shell: GNU errno7/7, POSIX errno7/7, signal50/50 (40 real faults); macOS errno7/7 also passes. These two issues are closed and must not be carried into the next spec. Do not describe prior graphics/code batches as uncommitted. User wants one integrated real-PKG release: full native host link, production loader/Orbis/FEX/VM/thread/renderer/input/audio, UI base+update install, interactive TMNT scene, ten-minute run, Stop and three same-process restarts. Continue internal dependency steps without another micro-spec. AYN Thor is auxiliary; Swan/API36/4KiB remains the target. The actual APK still runs CPU smoke; it now consumes the host DSO for production pad and explicit host initialization (see current baseline).
- **Default graphics driver: Android/bionic Turnip (user instruction 2026-09-12).** Defer system-driver compatibility; no silent system fallback. Current profile defaults/environment-only driver configuration and `CreateInstance`'s default DynamicLoader have NOT yet been wired to native Turnip. Integrate a pinned bionic package, actual adrenotools/loader handle and `vkGetInstanceProcAddr`, library/namespace lifetime and actual driver/ELF identity in PKG v2. Old `*-EMULATOR.zip` bundles may be glibc. Check shaderInt64 on the selected Turnip: the current shader backend unconditionally emits Int64. The reviewed system Adreno740 reports Vulkan1.3.128 + four required extensions but no Int64; that is not Turnip evidence.
- **Verified repairs:** commits `982cf0db`/`fe6f8fca` implement unified teardown, normal Prepare/Run exception handling, control RAII lease, late-drain ownership and long-lived Service polling/async onDestroy. Fresh SessionCore tests: 807/0. Preserve those fixes; remaining generation-finalize/destructor/Service stop-receipt and production lifetime contracts still apply. New graphics source fixes resolve bionic SpinLock fallback declarations, Android platform detection, glslang/header and generated-header closure. Complete VIDEO_CORE37 + SHADER_RECOMPILER67 now produce 104 AArch64 relocatable objects, verified individually; this is not a full linked host.
- **Build/allocator/WSI boundaries:** maintained census is `scripts/android/check-host-ndk-sources.py`; old sweep entries delegate to it, no `/tmp/host_inc.txt`, failures now return nonzero. Acquire does one finite driver call per frame, returns distinct states, timeout/cancel no longer forces recreation, and suboptimal/successful acquire still consumes its semaphore if Stop races. Production acquire helper tests19/0. Presenter::RequestStop exists but session/ANativeWindow generation/stop-before-join, waitIdle/fence cancellation and driver-error reporting remain to integrate. Actual FEX allocator remains rpmalloc; the current runtime uses ordinary mmap/munmap hooks without SetupHooks address stealing, as verified above. Preserve FEX child rules/pin `385a0cc4`; no Windows HookPtrs or maps-gap MAP_FIXED workaround. Darwin NDK builds real Android objects/shared libraries; desktop compiler issues do not justify a general Android build blocker.
- **CPU contract status:** the prior [N4 review](docs/validation/round2/g3-n4-review-2026-09-11.md) is historical at `e0693faa`. E0 runner/gate, E1-a RCX and E2-a native FP/Invoke catch repairs now exist; do not report the old individual defects as unchanged. HleScope/InvokeGuest/cancellable waits and nested fenv/errno/exception handling now exist (G45); the full formal matrices and simultaneous healthy HLE isolation must still be assessed by their specific requirements. Complete the binding [E1/E2/E3 requirements](docs/specs/android-fex-round2-n4-to-apk.md) before enabling corresponding callbacks. Preserve the per-owner NON-spill production syscall stop target: read ReturningStackLocation at the syscall point, never spill ABI-clobbered host values after C++ dispatch. `V0_BUILD_TESTS=OFF` excludes gate/trace while retaining that wrapper; Release alone is insufficient. Host-native graphics are a new stage and do not retroactively change Round 2 scope or acceptance.
- Preserve G2 pre-mutation shared/local/decoder retirement, poison, actual G27 stores, persistent owners/state, failed-drain retry, internal-only Pause retirement and sink/pin exclusions. Q1–Q3 and warm-owner G24 identity/epoch matrices remain pending; [full R2/R3/S0–S3 requirements](docs/specs/android-fex-round2-g3-repair-h3.md) remain binding. Never restore SleepThread parking, host SIGILL restoration or settle sleeps. Canonical is V0 10/0/47 plus3 deferred; all24 formal R2 cases NOT_RUN. Preserve historical evidence and dirty/untracked research/submodule work; complete the Git delivery set without backfilling old source identities. Swan ordinary APK is still G4; 16KiB/finite Step/Vulkan/game/VR deferred.
- Current priority (user update 2026-09-08): **Swan / Android 16 / ARM64 / 4 KiB host pages**. Defer 16 KiB adaptation and device acceptance; do not treat them as current blockers. The architecture remains native ARM64 shadPS4 host services with **FEXCore executing only PS4 x86-64 guest code**. Beat Saber means the PS4/PSVR release; one non-VR game is also a target.
- Read [the transaction hardening record](docs/validation/v0/transaction-hardening-2026-09-08.md) before implementation, then [the prior poison follow-up](docs/validation/v0/followup-poison-2026-09-08.md). The main adapter retains the CreateThread execution lease for the whole call; failed ranges are tracked independently; pinned-write invalidation failures and sink exceptions block execution; drain excludes replacement registration and mapping mutations cannot enter publication/execution. Swan contract 34/34 and real guest 45/45 pass. The archived runner reports 11 PASS / 0 FAIL / 46 NOT_RUN of 57 in-scope cases, with B04/M02/M03 deferred. This is not app acceptance: the closeout identifies B02's standalone ELF checks as auxiliary, and other host/CLI evidence still needs its required app environment. Round 2 G0 must correct artifact/environment/coverage accounting without rewriting old results. Build entry remains cmake/fex with guest_cpu_fex; scripts/android/build-fexcore-android delegates its embedder build to that entry. Still V0_IN_PROGRESS: G1 has CLI evidence only; real HLE/callback, app/JNI/Surface, bounded Step and debug integration remain incomplete.
- The repository still contains the desktop shadPS4 core, plus the new `android/shadps4-app` JNI/CPU smoke frontend. The pinned reference remains under `references/Bachata-S4-android/android/BachataS4`. The host library now links loader/Orbis HLE/renderer objects. Production loader/VM/thread/TLS/HLE execution through Android SessionCore/FEX is verified by the current synthetic APK; renderer/game execution remains unaccepted.
- **Native source progress:** RasterizerHooks reduces a direct memory/Vulkan include; time/UUID bionic source adaptations, AAudio source and Android Vk surface/acquire changes exist. Historical verification was mostly per-TU syntax; absence of direct includes is not a linked HLE closure. The app now loads the host DSO (AAudio/date/renderer/media linked), and its production backend now executes guest ELF/SELF as described above; continue game and renderer coverage. AAudio needs short-write/gain/open-failure handling. Guest ABI coverage and production surface ownership remain game integration work; VM geometry, typed registry and scoped TLS/callback composition are implemented above.
- **Real content is base + update:** the user-specified `/Users/bytedance/game/ps4/TMNT.Splintered.Fate_CUSA50828_v1.08.pkg` is CATEGORY `gp`, APP_VER01.08, SHA `c3ad762919ca1e2bbf781d856b842379b08d0a5e6ef71fff122f0d8a859e0af1`. A matching CATEGORY `gd` / 01.00 base exists under `/Users/bytedance/game/ps4/TMNT/CUSA50828/`, SHA `4ebea14adbabdd48c68875839f9e65800954a8a916247b1f353e8d76b414f2b6`. Exact paths and metadata are in [pkg-set.json](docs/validation/android-native-host/2026-09-12-review/pkg-set.json); the other nested1.08 file is byte-identical to the specified patch. No package was extracted or executed in the review. Reuse existing importer/overlay UI, add identity checks and recoverable update transactions; do not treat the patch as a standalone base or commit game data.
- Start with [the Android/ARM64 integration audit](docs/android-arm64-integration-audit.md), then [the implementation plan](docs/fex-android16-native-guest-host-plan.md). All research entry points are in [docs/README.md](docs/README.md).
- The fixed initial baseline is commit `a712889343ccd2588b712988dae0a76a104e0dce`; its [status record](docs/baselines/2026-09-07-android-fex-foundation.md) distinguishes committed source, validation and excluded local work. Keep that historical record intact and add new milestone records for later progress.
- [V0](docs/specs/android-fex-v0.md) retains the full normative API and acceptance requirements. The next implementation increment is Round 2 above; its completion does not imply V0_ACCEPTED. Specifications are requirements, not implementation evidence.
- Prioritize whether source interfaces can be integrated over small app version differences. The published app label is not proof of source or APK correspondence.

## Repository layout and reference ownership

- `src/`: this fork's emulator implementation; `externals/` and `foundation/`: build dependencies; `references/`: independently versioned source for comparison and migration. Do not overwrite the main core with a complete reference fork.
- Before changing a dependency, read [subrepository ownership](docs/subrepository-ownership.md). Android/ARM64 development branches live in `tencentmalos/Bachata-S4`; FEX now has a `tencentmalos/FEX` fork with separate old-runtime and new-audit branches. Reference gitlinks remain unchanged; branch existence is not proof of integration or permission to bypass contribution rules.
- Reuse [Spatial Foundation](docs/foundation-integration.md) for common reflection/packing, host diagnostics and networking. The `shadps4::foundation` target enables DebugBus (plus dumpsys on Android); host input separately links `spatial::foundation_input` and the application uses its Kotlin library. Reflection/network still require their actual NDK dependency and lifecycle audit; do not invent replacement frameworks or claim that they are already enabled. Guest CPU/Orbis semantics remain host-owned adapters.
- Foundation's development branch is `codex/shadps4-android-fex-v0`, based on the same library used by azahar. Push child changes before advancing the parent gitlink. Do not blindly import mimalloc TLS slot settings, fixed libevent configuration, or azahar-specific dependency paths.
- [references/README.md](references/README.md) records each reference's purpose, source and pinned revision. The authoritative checkout revisions are the Git gitlinks, not a remote branch's latest HEAD.
- Android UI/session/input reference: `references/Bachata-S4-android` at `67dbf4e5…`. It includes its own historical C++/runtime combination for controlled comparisons.
- ARM64 guest/HLE migration source: `references/shadps4-arm64` at `be6bc2e9…`. It contains no Android Gradle frontend.
- FEX checkout: `references/FEX` at current gitlink `385a0cc4d…` on the owned fork; original upstream research baseline was `50e6eee9…`. The two emulator references use **older FEX `f2b679f6…`** in their runtime locks. Keep API adaptation separate from Android integration; do not replace that pin blindly.
- `references/Bachata-S4` preserves the SG8275 bring-up fork. Local changes inside submodules are independent of parent commits. Inspect and preserve them; a parent gitlink does not save uncommitted child files.
- `references/dynarmic-citron` and `references/dynarmic-azahar` pin the two measured Dynarmic revisions. The comparison script defaults to these in-repository checkouts; full citron/azahar applications remain external references.
- Read applicable instructions within a reference before changing it. In particular, FEX's own `AGENTS.md` and `CLAUDE.md` prohibit AI-generated code contributions; its checkout is used for source inspection here.
- When changing a reference pin, verify that its configured remote contains that commit and update the reference index/evidence as needed. Avoid `git submodule update --remote` as an incidental setup step.

## Confirmed integration facts and hard constraints

1. The Android FEX launch path runs an **ARM64 glibc shadPS4 process with FEXCore linked inside**, not FEXLoader translating the whole x86 emulator. Guest/HLE veneers, ABI marshaling, pthread/TLS and callbacks already have implementations worth migrating.
2. The actual service control protocol is newline-delimited ASCII **`BACHATA/1`**. `RuntimeProtocol.kt`'s length-prefixed JSON is not the service's active C++ protocol. Do not infer pause/stop/debug support from unused scaffold types.
3. Both reference engines reject host pages other than 4096. Android 16 KiB support requires host allocation/protection, guard pages, JIT, VA probing and GPU-tracking work. **Never globally replace 4096 with 16384**: host pages, PS4 ABI granularity and internal tracking indices are distinct.
4. The reference ARM64 emulator build is Linux/glibc. The main repository has NDK/bionic FEX harnesses and a JNI CPU smoke APK; complete host integration, allocator ownership, guest pthread/TLS, lifecycle and dependency closure remain unvalidated.
5. The renderer now has an Android Surface creation branch, but app ANativeWindow handoff, generation/lifetime, safe cancellation and actual guest rendering are not integrated. The reference Android presentation copies AHB pixels through CPU buffers/Bitmap/Canvas. Native Surface/swapchain lifecycle is a separate implementation task.
6. Vortek client/server revisions differ between the references. A successful version handshake is insufficient proof of serialization/WSI compatibility. Preserve a matching set when reproducing the legacy path.
7. `package-runtime.mjs` can replace a newly built core with a local deep-guest pin. Use `BACHATA_DEEP_GUEST_PIN=0` when validating source iterations, and check the deployed binary's Build ID plus matching symbols.
8. Ordinary DS4 input is not PSVR/Move 6DoF support. Establish a non-VR execution/render/input/audio baseline before treating Beat Saber as a VR compatibility milestone.

## Debugging model

- Read [guest debugger feasibility](docs/fex-guest-debugger-feasibility.md) and [host LLDB to guest workflow](docs/fex-lldb-host-guest-workflow.md) before implementing debugger controls.
- Prefer host LLDB plus an explicit guest thread/state adapter first. Existing FEX Linux GDB-stub behavior does not constitute a ready shadPS4 guest debugger; several protocol operations are incomplete.
- A host stop is not automatically a guest instruction boundary. CPUState may be stale while guest values reside in JIT host registers. Distinguish a spilled safe-point snapshot from an asynchronous JIT stop.
- `STATE=x28` is specific to the audited ARM64 JIT context; it is not a universal process-register interpretation. Use the exact deployed FEX/layout version and host-PC-to-guest-RIP metadata.
- Keep inspection read-only where possible. Do not evaluate inferior helpers such as state reconstruction routines without checking side effects, locking and stop context.

## Validation and documentation

- Build instructions for the desktop core are under `documents/building-*.md`; existing tests are under `tests/`. Select checks for the actual implementation change.
- Foundation's standalone build probe is `tests/foundation`; see its [integration record](docs/foundation-integration.md) for commands. macOS smoke passed and an API 35 NDK shared library was built with 16 KiB ELF alignment; neither is Android 16 runtime validation. Locally labelled r28c/r29 installs both advertised max API 35 in their actual metadata: validate the sysroot and compiler, not just the directory/package label.
- Reproduce the reference transport checks with `python3 scripts/analysis/run_android_arm64_contract_tests.py`; required reference/dependency setup is in `references/README.md`. This compiles actual C++ runtime/controller/audio code and upstream tests.
- The recorded transport result is **18/18 on macOS ARM64**. Android Kotlin/Java, FEX execution, Vulkan, APK installation and 16 KiB device execution were not exercised by that test. Do not describe it as Android end-to-end validation.
- `scripts/analysis/compare_cpu_core_size.py` measures source text at Git revisions. Nonblank lines are not binary size, complete dependency size, effort or performance; preserve the stated counting scope.
- `docs/data/` holds dated source snapshots, lock metadata and bounded validation evidence. Historical paths/Build IDs in evidence are observations, not portable setup instructions.
- Use repository-relative links in committed documentation, and exact upstream commit links when citing external code. Keep findings, proposed design and verified runtime results clearly separated.
- Before committing, inspect both parent and submodule status, stage intended paths explicitly, and verify reference availability. Do not include game binaries, downloaded runtimes, build outputs, unrelated local checkouts or credentials.
