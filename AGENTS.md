# Repository context for coding agents

## Project objective and current state

- **Current execution: [COMMON/Git progress review](docs/validation/android-native-host/pkg-v2-common-review-2026-09-12.md) → [PKG v2 integrated execution spec](docs/specs/android-native-host-pkg-v2.md).** Graphics/WSI/script repairs are committed as `091334d3` and entry-point sync as `1d411955`. Code base `7c675280` includes COMMON/bionic adaptations `06bd43bd`. Main history plus omitted specs/research/evidence were pushed through `2efe7004` (archive commits `bb4d6c4a`/`2efe7004`); this latest review is delivered afterward. Fresh COMMON4/4 objects and crypto/ipc include checks pass. Both review findings are now fixed in the main repo: actual-return-type strerror_r handling and 16-byte ESR-record layout validation. Formal tests/common targets pass on AYN/API33/4KiB shell: GNU errno7/7, POSIX errno7/7, signal50/50 (40 real faults); macOS errno7/7 also passes. These two issues are closed and must not be carried into the next spec. Do not describe prior graphics/code batches as uncommitted. User wants one integrated real-PKG release: full native host link, production loader/Orbis/FEX/VM/thread/renderer/input/audio, UI base+update install, interactive TMNT scene, ten-minute run, Stop and three same-process restarts. Continue internal dependency steps without another micro-spec. AYN Thor is auxiliary; Swan/API36/4KiB remains the target. The actual APK still runs CPU smoke and has no complete native host link.
- **Default graphics driver: Android/bionic Turnip (user instruction 2026-09-12).** Defer system-driver compatibility; no silent system fallback. Current profile defaults/environment-only driver configuration and `CreateInstance`'s default DynamicLoader have NOT yet been wired to native Turnip. Integrate a pinned bionic package, actual adrenotools/loader handle and `vkGetInstanceProcAddr`, library/namespace lifetime and actual driver/ELF identity in PKG v2. Old `*-EMULATOR.zip` bundles may be glibc. Check shaderInt64 on the selected Turnip: the current shader backend unconditionally emits Int64. The reviewed system Adreno740 reports Vulkan1.3.128 + four required extensions but no Int64; that is not Turnip evidence.
- **Verified repairs:** commits `982cf0db`/`fe6f8fca` implement unified teardown, normal Prepare/Run exception handling, control RAII lease, late-drain ownership and long-lived Service polling/async onDestroy. Fresh SessionCore tests: 807/0. Preserve those fixes; remaining generation-finalize/destructor/Service stop-receipt and production lifetime contracts still apply. New graphics source fixes resolve bionic SpinLock fallback declarations, Android platform detection, glslang/header and generated-header closure. Complete VIDEO_CORE37 + SHADER_RECOMPILER67 now produce 104 AArch64 relocatable objects, verified individually; this is not a full linked host.
- **Build/allocator/WSI boundaries:** maintained census is `scripts/android/check-host-ndk-sources.py`; old sweep entries delegate to it, no `/tmp/host_inc.txt`, failures now return nonzero. Acquire does one finite driver call per frame, returns distinct states, timeout/cancel no longer forces recreation, and suboptimal/successful acquire still consumes its semaphore if Stop races. Production acquire helper tests19/0. Presenter::RequestStop exists but session/ANativeWindow generation/stop-before-join, waitIdle/fence cancellation and driver-error reporting remain to integrate. Actual FEX allocator is rpmalloc; retriable preflight does not close SetupHooks fatal recovery/ART ownership. Preserve FEX child rules/pin `385a0cc4`; no Windows HookPtrs or maps-gap MAP_FIXED workaround. Darwin NDK builds real Android objects/shared libraries; desktop compiler issues do not justify a general Android build blocker.
- **CPU contract status:** the prior [N4 review](docs/validation/round2/g3-n4-review-2026-09-11.md) is historical at `e0693faa`. E0 runner/gate, E1-a RCX and E2-a native FP/Invoke catch repairs now exist; do not report the old individual defects as unchanged. Full fenv/errno/exception/partial-pin/ABI matrices and simultaneous healthy HLE isolation remain open; no HleScope/InvokeGuest/WaitingHle exists. Complete the binding [E1/E2/E3 requirements](docs/specs/android-fex-round2-n4-to-apk.md) before enabling corresponding callbacks. Preserve the per-owner NON-spill production syscall stop target: read ReturningStackLocation at the syscall point, never spill ABI-clobbered host values after C++ dispatch. `V0_BUILD_TESTS=OFF` excludes gate/trace while retaining that wrapper; Release alone is insufficient. Host-native graphics are a new stage and do not retroactively change Round 2 scope or acceptance.
- Preserve G2 pre-mutation shared/local/decoder retirement, poison, actual G27 stores, persistent owners/state, failed-drain retry, internal-only Pause retirement and sink/pin exclusions. Q1–Q3 and warm-owner G24 identity/epoch matrices remain pending; [full R2/R3/S0–S3 requirements](docs/specs/android-fex-round2-g3-repair-h3.md) remain binding. Never restore SleepThread parking, host SIGILL restoration or settle sleeps. Canonical is V0 10/0/47 plus3 deferred; all24 formal R2 cases NOT_RUN. Preserve historical evidence and dirty/untracked research/submodule work; complete the Git delivery set without backfilling old source identities. Swan ordinary APK is still G4; 16KiB/finite Step/Vulkan/game/VR deferred.
- Current priority (user update 2026-09-08): **Swan / Android 16 / ARM64 / 4 KiB host pages**. Defer 16 KiB adaptation and device acceptance; do not treat them as current blockers. The architecture remains native ARM64 shadPS4 host services with **FEXCore executing only PS4 x86-64 guest code**. Beat Saber means the PS4/PSVR release; one non-VR game is also a target.
- Read [the transaction hardening record](docs/validation/v0/transaction-hardening-2026-09-08.md) before implementation, then [the prior poison follow-up](docs/validation/v0/followup-poison-2026-09-08.md). The main adapter retains the CreateThread execution lease for the whole call; failed ranges are tracked independently; pinned-write invalidation failures and sink exceptions block execution; drain excludes replacement registration and mapping mutations cannot enter publication/execution. Swan contract 34/34 and real guest 45/45 pass. The archived runner reports 11 PASS / 0 FAIL / 46 NOT_RUN of 57 in-scope cases, with B04/M02/M03 deferred. This is not app acceptance: the closeout identifies B02's standalone ELF checks as auxiliary, and other host/CLI evidence still needs its required app environment. Round 2 G0 must correct artifact/environment/coverage accounting without rewriting old results. Build entry remains cmake/fex with guest_cpu_fex; scripts/android/build-fexcore-android delegates its embedder build to that entry. Still V0_IN_PROGRESS: G1 has CLI evidence only; real HLE/callback, app/JNI/Surface, bounded Step and debug integration remain incomplete.
- The repository still contains the desktop shadPS4 core, plus the new `android/shadps4-app` JNI/CPU smoke frontend. The pinned reference remains under `references/Bachata-S4-android/android/BachataS4`. An integrated loader/Orbis HLE/renderer NDK host has **not** been implemented or validated here.
- **Native source progress:** RasterizerHooks reduces a direct memory/Vulkan include; time/UUID bionic source adaptations, AAudio source and Android Vk surface/acquire changes exist. Historical verification was mostly per-TU syntax; absence of direct includes is not a linked HLE closure. The app still links CPU smoke only; include real AAudio/date/renderer/media dependencies and finish the reusable host CMake targets. AAudio needs short-write/gain/open-failure handling. Guest native-pointer calls, VM 64GiB geometry, typed registry, TLS/callback and production surface ownership remain integration work.
- **Real content is base + update:** the user-specified `/Users/bytedance/game/ps4/TMNT.Splintered.Fate_CUSA50828_v1.08.pkg` is CATEGORY `gp`, APP_VER01.08, SHA `c3ad762919ca1e2bbf781d856b842379b08d0a5e6ef71fff122f0d8a859e0af1`. A matching CATEGORY `gd` / 01.00 base exists under `/Users/bytedance/game/ps4/TMNT/CUSA50828/`, SHA `4ebea14adbabdd48c68875839f9e65800954a8a916247b1f353e8d76b414f2b6`. Exact paths and metadata are in [pkg-set.json](docs/validation/android-native-host/2026-09-12-review/pkg-set.json); the other nested1.08 file is byte-identical to the specified patch. No package was extracted or executed in the review. Reuse existing importer/overlay UI, add identity checks and recoverable update transactions; do not treat the patch as a standalone base or commit game data.
- Start with [the Android/ARM64 integration audit](docs/android-arm64-integration-audit.md), then [the implementation plan](docs/fex-android16-native-guest-host-plan.md). All research entry points are in [docs/README.md](docs/README.md).
- The fixed initial baseline is commit `a712889343ccd2588b712988dae0a76a104e0dce`; its [status record](docs/baselines/2026-09-07-android-fex-foundation.md) distinguishes committed source, validation and excluded local work. Keep that historical record intact and add new milestone records for later progress.
- [V0](docs/specs/android-fex-v0.md) retains the full normative API and acceptance requirements. The next implementation increment is Round 2 above; its completion does not imply V0_ACCEPTED. Specifications are requirements, not implementation evidence.
- Prioritize whether source interfaces can be integrated over small app version differences. The published app label is not proof of source or APK correspondence.

## Repository layout and reference ownership

- `src/`: this fork's emulator implementation; `externals/` and `foundation/`: build dependencies; `references/`: independently versioned source for comparison and migration. Do not overwrite the main core with a complete reference fork.
- Before changing a dependency, read [subrepository ownership](docs/subrepository-ownership.md). Android/ARM64 development branches live in `tencentmalos/Bachata-S4`; FEX now has a `tencentmalos/FEX` fork with separate old-runtime and new-audit branches. Reference gitlinks remain unchanged; branch existence is not proof of integration or permission to bypass contribution rules.
- Reuse [Spatial Foundation](docs/foundation-integration.md) for common reflection/packing, host diagnostics and networking. The current `shadps4::foundation` target enables only DebugBus (plus dumpsys on Android). Reflection/network still require their actual NDK dependency and lifecycle audit; do not invent replacement frameworks or claim that they are already enabled. Guest CPU/Orbis semantics remain host-owned adapters.
- Foundation's development branch is `codex/shadps4-android-fex-v0`, based on the same library used by azahar. Push child changes before advancing the parent gitlink. Do not blindly import mimalloc TLS slot settings, fixed libevent configuration, or azahar-specific dependency paths.
- [references/README.md](references/README.md) records each reference's purpose, source and pinned revision. The authoritative checkout revisions are the Git gitlinks, not a remote branch's latest HEAD.
- Android UI/session/input reference: `references/Bachata-S4-android` at `67dbf4e5…`. It includes its own historical C++/runtime combination for controlled comparisons.
- ARM64 guest/HLE migration source: `references/shadps4-arm64` at `be6bc2e9…`. It contains no Android Gradle frontend.
- FEX checkout: `references/FEX` at current gitlink `385a0cc4d…` on the owned fork; original upstream research baseline was `50e6eee9…`. The two emulator references use **older FEX `f2b679f6…`** in their runtime locks. Keep API adaptation separate from Android integration; do not replace that pin blindly.
- `references/Bachata-S4` preserves the SG8275 bring-up fork. Local changes inside submodules are independent of parent commits. Inspect and preserve them; a parent gitlink does not save uncommitted child files.
- `references/dynarmic-citron` and `references/dynarmic-azahar` pin the two measured Dynarmic revisions. The comparison script defaults to these in-repository checkouts; full citron/azahar applications remain external references.
- Read applicable instructions within a reference before changing it. In particular, FEX's own `AGENTS.md` and `CLAUDE.md` prohibit AI-generated code contributions; its checkout is used for source inspection here.
- When changing a reference pin, verify that its configured remote contains that commit and update the reference index/evidence as needed. Avoid `git submodule update --remote` as an incidental setup step.

## Confirmed integration facts and hard constraints

1. The Android FEX launch path runs an **ARM64 glibc shadPS4 process with FEXCore linked inside**, not FEXLoader translating the whole x86 emulator. Guest/HLE veneers, ABI marshaling, pthread/TLS and callbacks already have implementations worth migrating.
2. The actual service control protocol is newline-delimited ASCII **`BACHATA/1`**. `RuntimeProtocol.kt`'s length-prefixed JSON is not the service's active C++ protocol. Do not infer pause/stop/debug support from unused scaffold types.
3. Both reference engines reject host pages other than 4096. Android 16 KiB support requires host allocation/protection, guard pages, JIT, VA probing and GPU-tracking work. **Never globally replace 4096 with 16384**: host pages, PS4 ABI granularity and internal tracking indices are distinct.
4. The reference ARM64 emulator build is Linux/glibc. The main repository has NDK/bionic FEX harnesses and a JNI CPU smoke APK; complete host integration, allocator ownership, guest pthread/TLS, lifecycle and dependency closure remain unvalidated.
5. The renderer now has an Android Surface creation branch, but app ANativeWindow handoff, generation/lifetime, safe cancellation and actual guest rendering are not integrated. The reference Android presentation copies AHB pixels through CPU buffers/Bitmap/Canvas. Native Surface/swapchain lifecycle is a separate implementation task.
6. Vortek client/server revisions differ between the references. A successful version handshake is insufficient proof of serialization/WSI compatibility. Preserve a matching set when reproducing the legacy path.
7. `package-runtime.mjs` can replace a newly built core with a local deep-guest pin. Use `BACHATA_DEEP_GUEST_PIN=0` when validating source iterations, and check the deployed binary's Build ID plus matching symbols.
8. Ordinary DS4 input is not PSVR/Move 6DoF support. Establish a non-VR execution/render/input/audio baseline before treating Beat Saber as a VR compatibility milestone.

## Debugging model

- Read [guest debugger feasibility](docs/fex-guest-debugger-feasibility.md) and [host LLDB to guest workflow](docs/fex-lldb-host-guest-workflow.md) before implementing debugger controls.
- Prefer host LLDB plus an explicit guest thread/state adapter first. Existing FEX Linux GDB-stub behavior does not constitute a ready shadPS4 guest debugger; several protocol operations are incomplete.
- A host stop is not automatically a guest instruction boundary. CPUState may be stale while guest values reside in JIT host registers. Distinguish a spilled safe-point snapshot from an asynchronous JIT stop.
- `STATE=x28` is specific to the audited ARM64 JIT context; it is not a universal process-register interpretation. Use the exact deployed FEX/layout version and host-PC-to-guest-RIP metadata.
- Keep inspection read-only where possible. Do not evaluate inferior helpers such as state reconstruction routines without checking side effects, locking and stop context.

## Validation and documentation

- Build instructions for the desktop core are under `documents/building-*.md`; existing tests are under `tests/`. Select checks for the actual implementation change.
- Foundation's standalone build probe is `tests/foundation`; see its [integration record](docs/foundation-integration.md) for commands. macOS smoke passed and an API 35 NDK shared library was built with 16 KiB ELF alignment; neither is Android 16 runtime validation. Locally labelled r28c/r29 installs both advertised max API 35 in their actual metadata: validate the sysroot and compiler, not just the directory/package label.
- Reproduce the reference transport checks with `python3 scripts/analysis/run_android_arm64_contract_tests.py`; required reference/dependency setup is in `references/README.md`. This compiles actual C++ runtime/controller/audio code and upstream tests.
- The recorded transport result is **18/18 on macOS ARM64**. Android Kotlin/Java, FEX execution, Vulkan, APK installation and 16 KiB device execution were not exercised by that test. Do not describe it as Android end-to-end validation.
- `scripts/analysis/compare_cpu_core_size.py` measures source text at Git revisions. Nonblank lines are not binary size, complete dependency size, effort or performance; preserve the stated counting scope.
- `docs/data/` holds dated source snapshots, lock metadata and bounded validation evidence. Historical paths/Build IDs in evidence are observations, not portable setup instructions.
- Use repository-relative links in committed documentation, and exact upstream commit links when citing external code. Keep findings, proposed design and verified runtime results clearly separated.
- Before committing, inspect both parent and submodule status, stage intended paths explicitly, and verify reference availability. Do not include game binaries, downloaded runtimes, build outputs, unrelated local checkouts or credentials.
